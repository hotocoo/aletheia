# GAPS4 Disposition Register

Machine-readable disposition of every finding in `ARCHITECTURE-GAPS4.md` (the comprehensive
repository audit). This register is the backlog tracker and the mechanism that keeps status claims
from drifting (ALET-P2-011) — every finding is either **resolved** (with evidence that exists in the
tree), **open** (queued, not yet started), or **deferred** (explicitly ADR-scoped as future
milestone work, carrying no false "done" claim).

**Disposition legend:** `resolved` — fix landed + verified; `open` — accepted, queued;
`deferred` — real work, scoped to a later milestone via ADR/roadmap (not counted as delivered).

**As of:** 2026-08-09 (third wave).

| ID | Sev | Disposition | Evidence / Note |
|----|-----|-------------|-----------------|
| ALET-P0-001 | P0 | resolved | x86-64 boot gate at CI parity: `kernel-x86_64/scripts/build-image-linux.sh` (portable mtools ESP), `scripts/vm-e2e-x86.sh`, `.github/workflows/ci.yml` job `vm-e2e-x86`; boots exit 33 + 22 ring-3 markers |
| ALET-P0-002 | P0 | resolved | `scripts/build-all.sh` (every crate, own toolchain/target, host crates tested) + CI job `build-all`; `E2E-ALL: PASS` 3 targets |
| ALET-P0-003 | P0 | resolved | `scripts/check-ci-parity.sh` (REQ-QUAL-001) + CI job `ci-parity` in BOTH pipelines: FS-discovered bootable crates must each have a CI-executed boot gate, GitHub↔GitLab script sets must match, every matrix `VM Gate` must actually run, STATUS↔CI cross-check. Wiring it exposed and fixed 3 real gaps (GitLab missing `build-all`/`vm-e2e-x86`; `conformance.sh` claimed but never CI-run and macOS-gated on x86) |
| ALET-P1-001 | P1 | resolved | `kernel-core/src/vmaddr.rs` (REQ-MM-001, ADR-029) enforced at every mapping API on all three targets; host property proof `kernel-core/tests/vmaddr.rs` (no accepted VA pair aliases; every accepted PA is an owned frame) + 8/8/7 live-table VM invariants (aarch64 21, riscv64 21, x86-64 13 vm invariants) + refusals added to the conformance core contract |
| ALET-P1-002 | P1 | resolved | `kernel-core/src/ptreclaim.rs` (REQ-MM-003, ADR-031): an unmap that empties a table frees it — empty-only, parent cleared before the free, root never freed, stop at the first table in use, refused free restores the reference. Wired into all three targets' unmap paths; host-proved against an in-memory table model + VM-gated (aarch64/RISC-V 33 vm invariants, x86-64 25) + five reclamation behaviors added to the conformance core contract |
| ALET-P1-003 | P1 | resolved | `kernel-core/src/frameown.rs` (REQ-MM-002, ADR-030): one owner per frame, claimed/released through the allocator on all three targets; host property proof `kernel-core/tests/frameown.rs` (no frame held twice; counters balance; every refusal is a no-op, asserted after each of 20 000 deterministic ops) + 17 memory invariants per target in the VM gates (was 7) + the five ownership refusals added to the conformance core contract |
| ALET-P1-004 | P1 | resolved | `kernel-core/src/teardown.rs` (REQ-MM-004, ADR-032): a dying space returns every page/table/root it owns and nothing else, behind two independent guards (per-target privacy predicate + the ownership model); destroying the ACTIVE space is refused everywhere. Host-proved against an in-memory model + VM-gated on live hierarchies (aarch64/RISC-V 42 vm invariants, x86-64 33) with the frame count returning EXACTLY to its pre-space value + five teardown behaviors in the conformance core contract |
| ALET-P1-005 | P1 | resolved | the shootdown contract is WRITTEN and adversarially tested: `docs/INVARIANT-CONTRACTS.md` §INV-TLB states six invariants (all-acknowledged completion; an ack never precedes the invalidation it covers; exactly-once performance; no borrowing another requester's acks; an aborted wait is `false` and never a partial success; out-of-range targets ignored, never waited on), each naming its proof in `kernel-core/tests/shootdown.rs`. The new cases attack the barrier rather than exercise it — a silent core, a borrowed acknowledgement, a bogus target id — including a check made from INSIDE `perform` that the ack counter has not yet advanced. 10 host tests |
| ALET-P1-006 | P1 | resolved | all three parts (REQ-MM-008, ADR-040, contract §INV-LAYOUT). **Layout constants:** `kernel-core/src/layout.rs` — a target DECLARES named regions with their privilege and `Layout::validate` refuses a declaration that overlaps, is unaligned, contains the null page, or lets a user region merely ABUT a kernel one (something that grows would cross without ever being unmapped); every boot suite runs the validation. **Guard pages:** see ALET-P1-012. **KASLR posture:** none, deliberately, with the reason written down rather than the absence implied — every target identity-maps, which is what keeps the DMA story auditable, so randomizing the kernel base is a different memory model, not a flag; and KASLR defends against reading a pointer and using it, whereas every effect is capability-gated. **Writing the check found a real hole:** VA 0 was MAPPED by every boot identity map (device memory on aarch64/RISC-V, RAM on x86-64), so a kernel null dereference read or WROTE real state instead of faulting — `vmaddr` refused the null page at the mapping APIs but nothing had audited the boot map. Now punched out on all three targets and in the conformance contract |
| ALET-P1-007 | P1 | resolved | W^X is a GLOBAL invariant of the kernel-built address space on both QEMU targets (REQ-MM-006, ADR-034 + its 2026-08-03 addendum). `kernel-core/src/memattr.rs` validates permissions at every dynamic mapping API on all three targets; on top of that, `linker.ld` now exports `__text_start`/`__text_end`/`__rodata_end` and `build_identity` builds every RAM block overlapping the image as 4 KiB pages — text RO+X, `.rodata` RO+NX, data/bss/stack/heap RW+NX, RAM outside the image RW+NX blocks. Both QEMU gates require `dynamic_violations == 0` **and** `bootstrap_violations == 0` (was `== 64`), plus four invariants that assert the split is real: the leaf over `__text_start` is a 4 KiB page, text is executable and read-only, `.rodata` is neither, data+stack are RW+NX. Because the split replaced the block descriptors that had implicitly made image VAs unmappable-over, `map_page`/`unmap_page` now refuse the block-aligned split span explicitly (verified: the two new invariants fail at 54 with `exit 114` against the pre-guard code). Virtual-memory invariants 49 → 55 on aarch64 and RISC-V (1087 / 576 live leaves audited, 0/0 violations). The x86-64 firmware-inherited tree is tracked separately as ALET-P1-031 |
| ALET-P1-008 | P1 | resolved | permissions decoded and validated per-arch at every mapping API (REQ-MM-006, ADR-034): aarch64 AP/UXN/PXN/AttrIndx, RISC-V R/W/X/U, x86-64 WRITABLE/NO_EXECUTE/USER_ACCESSIBLE with EFER.NXE + CR4.SMEP enabled after a CPUID check and reported at boot. Cacheability is enforced where the ISA expresses it (aarch64 AttrIndx ⇒ device memory is never executable) and explicitly modelled as Normal where it does not (RISC-V PMAs, x86-64 PAT/MTRRs) rather than silently assumed. Four attribute behaviors in the conformance core contract |
| ALET-P1-009 | P1 | resolved | x86-64 trap-frame layout hardening, BOTH halves (ADR-039, REQ-FAULT-003). **Static half (2026-08-03):** the `TrapFrame` const-assert block in `kernel-x86_64/src/usermode.rs` pins size, alignment, the register array's offset and width, the named register indices, every `iretq`-frame offset the assembly's literals use, and that nothing hides past the last field. **Fuzz half (2026-08-09):** the static asserts pin the LAYOUT and cannot pin the trap path's BEHAVIOR over it — a save/restore pair that swapped two registers consistently satisfies every offset assert and still corrupts every task it interrupts. `run_register_roundtrip` primes all 15 registers with distinct non-address sentinels (`regfuzz_sentinel`), the stub traps IMMEDIATELY via `SYS_REGCHECK` so what the kernel sees is exactly what `resume_frame` loaded, the dispatcher compares all 15 saved slots plus CS/SS ring bits, RFLAGS and RSP-in-stack, and then the SAME frame is RESUMED and trapped again — which is what proves the restore direction rather than assuming it. Only the FIRST mismatch across both passes is reported, so a clean second pass cannot launder a failed first one. Three invariants (28-30) on the x86-64 boot, proved under BOTH hypervisors (QEMU+OVMF and VirtualBox) |
| ALET-P1-010 | P1 | resolved | re-entry is now DETECTABLE and fatal (REQ-FAULT-002, ADR-039, contract §INV-REENTRY). `kernel-core/src/reentry.rs` wraps a section a handler must not re-enter; entering while entered returns `None` and the x86-64 fault-report path then prints one line and exits 106 instead of recursing until the stack runs out and the machine triple-faults. The counter is a compare-exchange, so the same guard also catches a SECOND CPU entering a section with no lock (a different bug, same consequence), and refusals are counted so a caller that swallows one still leaves evidence. Five invariants host-proved in `kernel-core/tests/reentry.rs` — including a real two-thread barrier race — plus x86-64 boot invariant 58 proving the guard behaves inside the kernel |
| ALET-P1-011 | P1 | resolved | interrupt/fault ENTRY adversarial testing (ADR-039, REQ-FAULT-003). **Decode surface (2026-08-03):** `kernel-core/tests/faultclass.rs` sweeps every input the three architectures can present and asserts the fail-closed properties over the whole space — which proves the CLASSIFIER is fail-closed and says nothing about whether a real illegal opcode or a real privileged instruction executed at CPL 3 reaches a handler at all. **Entry half (2026-08-09):** two vectors that were fatal catch-alls now take a deliberate ring-3 hit each. `stub_ud` executes `ud2` (#UD, vector 6); `stub_gp` executes `hlt`, privileged at CPL 3 (#GP, vector 13) — chosen over `cli` because `cli`'s outcome depends on RFLAGS.IOPL and an invariant that changes meaning with a flag the test also sets is not an invariant. `isr_ud_entry` uses the full save-first macro (no error code); `isr_gp_entry` deliberately does NOT, because #GP pushes an error code ahead of the iretq frame and `save_frame` would read it as RIP and shift every field after it. Both route through one shared `supervise_ring3_fault` — three entry paths carrying three copies of the policy is three places for it to diverge. `idt::restore_fatal_traps()` hands both vectors back immediately afterwards: a safety net taken down for a test has to go back up, or a kernel-side #UD would run the containment path. Four invariants (31-34) including that a later ring-3 task still runs to completion after both attacks |
| ALET-P1-012 | P1 | resolved | guard pages on ALL THREE targets, not only x86-64 (REQ-MM-007, ADR-040). aarch64/RISC-V reserve `__stack_guard` one page below `__stack_bottom` in `linker.ld`; x86-64 makes the guard the first page of the page-aligned `KSTACK` static and moves `RSP0` above it. Each identity map SPLITS the containing block (a 2 MiB block or a megapage cannot have a hole) and leaves that page with no descriptor. Four `guard:` invariants per target prove it on the LIVE tree — no translation, no leaf at any level, the stack's own pages still mapped (a guard that cost the kernel its stack would be worse than none), and the stack still W^X — asserted against the KERNEL's own map rather than whatever CR3 holds at that point in the suite, a distinction the first version got wrong and the failure caught. Not claimed: no guards around the heap or the SMP secondaries' stacks, and derived per-process spaces are a named follow-on |
| ALET-P1-013 | P1 | resolved | `kernel-core/src/faultclass.rs` (REQ-FAULT-001, ADR-039, contract §INV-FAULT): a normalized `Fault` (present/write/user/exec/reserved/from-kernel) that x86-64, aarch64 (EC + DFSC + WnR) and RISC-V (`scause` + caller-supplied privilege/presence, because the ISA reports neither) decode into; a `FaultKind` for what it MEANS; and a fail-closed `FaultVerdict` where only user faults are survivable. A reserved bit dominates every other reading (corrupt page tables ⇒ never resumable), and an architectural bit the model does not interpret makes the fault `Unknown` — fatal — rather than being classified from the bits that happen to be understood, which is what makes the model safe to extend. Even the default `Fault` is a kernel fault, so a decoder that forgets a field cannot make one look routine. Six invariants proved EXHAUSTIVELY on the host (not sampled) + wired live on x86-64 (`idt.rs` classifies before reporting; boot invariants 56–57). Wiring scope stated: aarch64/RISC-V decoders are host-proved but not yet wired into those handlers |
| ALET-P1-014 | P1 | resolved | scheduler multicore contention DEPTH (host proofs in kernel-core/tests/smpsched.rs): a dynamic producer/consumer STORM - four producers racing four consumers through least-loaded placement AND explicit-queue seeding concurrently - held exactly-once for 4000 tasks with every queue drained and every ADR-028 lock-tripwire counter back to zero afterwards; an AFFINITY-under-steal-storm proof with mixed masks (pins, two-CPU windows, ANY) seeded deliberately onto mismatched queues, where every dispatch respected its mask, every runnable task ran exactly once, and EMPTY-mask tasks were refused placement outright, changed nothing, and were never lost; and the tripwire counters themselves became a measured claim via a test-only `debug_locks_held` accessor asserted zero after every storm (an unbalanced enter/exit pair anywhere on the dispatch path would show). Writing the storm exposed a bug IN THE TEST'S OWN exit condition - consumers quitting on executed >= produced, a moving target mid-storm - which losing 16 tasks made visible immediately |
| ALET-P1-015 | P1 | resolved | `docs/INVARIANT-CONTRACTS.md` §INV-TASK states five lifecycle invariants and `kernel-core/tests/sched.rs` attacks each: `Finished` is terminal under every following event (swept over all four states as interference); a Blocked task is never dispatched and only `unblock` makes it eligible; at most ONE task is Running and `current()` names exactly it, re-checked after each of 500 random events; `runnable_len` equals the rotation (Ready plus the Running task, never Blocked or Finished); and an event naming an unspawned task changes nothing. **The last one found a real defect:** `block`/`finish` INVENTED a task in the state table from a stray id, so the scheduler could believe in a task it never created and try to resume a context that does not exist — fixed in `sched.rs` in the same commit. Writing INV-TASK-4 also corrected the CONTRACT rather than the code: `runnable_len` counts the rotation, and the first draft of the invariant asserted Ready-only |
| ALET-P1-016 | P1 | resolved | `docs/INVARIANT-CONTRACTS.md` §INV-PRIO states seven invariants and `kernel-core/tests/priosched.rs` attacks each (16 host tests): a holder is never weaker than anyone blocked on an endpoint it holds — re-checked after EVERY operation against a tracked waiter model, not once at the end; donation is transitive along a whole A→B→C chain; donation ENDS at release (a holder keeping donated priority is a scheduling escalation); donation never manufactures priority above the highest base, proven over a dense tangle of holds and waits; `schedule_next` never dispatches a Blocked task nor a weaker Ready task over a stronger one; a donation CYCLE (deadlock) terminates instead of recursing; and an unauthorized `acquire`/`wait` changes no state at all — otherwise any task could force a donation onto a holder |
| ALET-P1-017 | P1 | resolved | `docs/INVARIANT-CONTRACTS.md` §INV-IPC-CANCEL states six invariants and `kernel-core/tests/ipc.rs` proves each: a cancelled message is never delivered by ANY later receive; cancelling something already delivered/expired/cancelled/forged returns false and changes nothing (a `true` return is the sender's evidence it won the race, so a lie there is worse than a refusal); cancellation removes exactly the named message and preserves order; every message reaches EXACTLY ONE terminal trace event (Recv | Expired | Cancel), audited over a mixed population; a cancelled slot frees exactly one unit of bounded capacity without lifting the bound; and a deadline and a cancel never both claim the same message, in either order |
| ALET-P1-018 | P1 | open | DMA isolation model. **Software boundary DELIVERED (ADR-043), the IOMMU CONTRACT DELIVERED (ADR-071), FIRST HARDWARE RUNG DELIVERED on x86-64 (ADR-073: VT-d, dmar=12, device-side walk evidence from the fault bank), and SECOND HARDWARE RUNG DELIVERED on aarch64 up to the emulator boundary (ADR-074): SMMUv3 discovered through the firmware-published device tree - delivered as an opt/ fw_cfg item, since direct -kernel ELF boots get no DTB pointer at all (measured x0=0) -, stage-2-only identity domain over OWNED frames minus image, every present PCI function granted an STE under its DECLARED iommu-map stream id, stream table + queues published with readback, enforcement enabled and latched layered over the software registry: a 10-invariant boot gate (smmu=10) plus host proofs of the walker semantics against SMMUv3 shapes. **Still open:** DEVICE-SIDE WALK PROBES on ARM - measured artifact: on QEMU 11.1 a CLI-attached virtio-blk-pci does not route DMA through the legacy iommu=smmuv3 unit (abort-all STEs leave completions intact); grant-serves/revocation-events need hotplug-after-done, an emulator fix, or real silicon. THIRD HARDWARE RUNG DELIVERED on x86-64 (ADR-075): PER-DEVICE WINDOWS - every driven function gets its own second-level tree containing exactly the frames ITS driver registry grants (leaf-set equality audited live), ungranted functions read back with NO context entry, grant sets pairwise disjoint, and page-granular revocation is denied BY NAME (measured reason 6 at exactly the revoked address) with sibling windows spared and restore-to-silence proved: dmar 12 -> 14, host proofs 12 -> 15. Also fixed en route: the ADR-074 seam mapped [bar_base, len) instead of bar_base+offset, leaving device-cfg unmapped on q35 - an infinite mis-labelled ring-3 fault loop that red-ed every target gate (commit fix(pci)). Still open: SMMUv3 per-stream WINDOWS on aarch64, DEVICE-SIDE WALK PROBES on ARM (measured QEMU 11.1 artifact: CLI-attached virtio-blk-pci does not route DMA through the legacy iommu=smmuv3 unit), interrupt remapping, queued invalidation, pass-through types, post-enable completion assertions. |
| ALET-P1-019 | P1 | resolved | virtio-blk production-grade test depth CLOSED (ADR-070 companion proofs, host device-model). The driver now validates its completions in FULL before reporting success: the used-ring entry's `id` must name the descriptor head it was posted under and its byte count must be EXACTLY what the chain promised (status + whole block for a READ; status alone otherwise) - so a SHORT read is refused BEFORE any byte reaches the caller buffer, a padded WRITE or FLUSH is refused, a completion for another descriptor's request is refused, and garbage status bytes are refused even with an exact count. `VirtioBlk::reset` restores service after a failure through full renegotiation over the SAME frames, idempotently. Sub-sector/partial-block calls stay refused at the API boundary (`BadBlockSize`, `OutOfRange`). Proved by a host-side virtio-mmio DEVICE MODEL that runs the device side synchronously and can LIE per-field: kernel-core/tests/virtioblk.rs gains ten tests including an exhaustive short/zero/long/garbage-status/id-lie/silent matrix, the poll-bound timeout path, reset recovery + idempotence, and cross-store confusion - every malformed run must refuse BOTH ops with nothing in the caller buffer, every accepted completion must carry exactly the right bytes. Named residual: fuzzing against REAL hardware beyond QEMU's honest device remains VM-gate coverage |
| ALET-P1-020 | P1 | resolved | `docs/INVARIANT-CONTRACTS.md` §INV-STORE-ERR states four invariants and `kernel-core/tests/storage.rs` proves each against a device that fails one specific operation: the error KINDS are distinguishable (wrong buffer size / block off the device / device failure / over-size transaction are four values, not aliases); a device error is SURFACED — including a failed **flush**, which is the durability barrier, so swallowing it would report durability that does not exist — and a failed commit leaves the home block untouched; the filesystem PRESERVES the cause (`FsError::Storage(Device)`) while its own refusals keep their own names, so a caller can tell "your request was wrong" from "the hardware failed"; and every refusal is a no-op, proven by comparing the whole device image byte-for-byte before and after, which is the strongest available form of "nothing happened" |
| ALET-P1-021 | P1 | resolved | the sandbox is bounded in EVERY dimension, each with a number, a refusal and a name (ADR-065). Fuel (REQ-COMP-002) measured only COMPUTE; `SandboxLimits` (`aletheia/src/component.rs`) bounds what fuel does not: linear-memory BYTES and table ELEMENTS through wasmi's store limiter scoped to the one-run store (growth past a cap fails as `-1`, so a respectful guest keeps running inside its cap while a deaf guest that ignores the refusal never gets another byte and dies of fuel instead — memory never exceeds the cap either way), instance/table/memory COUNTS pinned to 1, operand-stack HEIGHT and recursion DEPTH as engine compilation limits set per run (exceeding either traps `StackOverflow` → `KillReason::Stack`), and a WALL-CLOCK deadline enforced at every host-call crossing — a late crossing audits a DEADLINE row and refuses by trap, so nothing further is authorized on a clock the run has outrun. There is no unbounded constructor: `defaults()` caps all five dimensions (4 MiB / 1024 elements / 16 Ki slots / 256 frames / 30 s) and opting out must be WRITTEN (`deadline_ms: 0`). Every outcome now NAMES which bound held (`killed_by`: fuel/deadline/stack) in the outcome and in the `ComponentRan` audit event, because "an error" makes the three indistinguishable; an overrun a pure-compute guest managed to FINISH inside is still reported in `deadline_exceeded` rather than reading as compliance. Composition follows capability: the whole spawn tree runs inside the ROOT's envelope, so budget narrows down the tree exactly as attenuated authority does. Host-proved by `aletheia/tests/component_resources.rs` (10 tests): caps hold EXACTLY (a guest counting its wins reports precisely the configured page/element budget); depth enforced exactly as configured both ways; the clock-eater's last audit row IS the refused crossing; defaults bound everything; a spawned child inherits the root envelope. Named non-claims: a single host-call BODY is not clock-interruptible (bounded by store-operation granularity); per-component limits in installation records are future work — `run_installed` deliberately uses defaults rather than pretending otherwise |
| ALET-P1-022 | P1 | resolved | the component DECLARES what it speaks (ADR-066): a custom section "aletheia.abi" carrying the version as four little-endian bytes, enforced at BOTH gates - install compiles the module (never instantiates, no guest code runs) and refuses undeclared/malformed/foreign-version modules BEFORE their bytes are stored, auditing the refusal as ComponentInstallRefused and stamping admitted versions into entity metadata; run re-checks on every path before any guest state exists. Refusals are BY NAME with both sides of a version disagreement reported (component declares ABI v999, host speaks v1). The SDK stamps guests automatically from its own ABI_VERSION, verified END-TO-END by rebuilding the real example with the real wasm32 toolchain - the committed fixture grew the section and passes the unchanged gates. The v1 import surface itself is pinned by a LIVE probe: all four documented signatures must still link against the host linker, so a signature change without a version bump fails the suite loudly. Host proofs: aletheia/tests/component_abi.rs (8 tests). Named non-claims: exactly ONE version is spoken (no compatibility shims for old guests - refusing is fail-closed; adaptation would be a deliberate later decision), and the declaration proves what a binary WAS BUILT against, not what it contains beyond that |
| ALET-P2-049 | P2 | resolved | the example-component build script compiled with whichever rustc sat first on PATH: a workstation preferring Homebrew rust over rustup got E0463 (no wasm32 core in THAT sysroot) far from the cause - cargo resolves rustc from PATH even when cargo itself comes from rustup, so pinning cargo alone was not enough. scripts/build-example-component.sh now pins BOTH binaries via ` + "`rustup which cargo`/`rustup which rustc`" + `. Same family as the gate-runner defects ALET-P2-037/038: the harness is part of the claim |
| ALET-P1-023 | P1 | resolved | the supply chain is VERIFIED, LIVE, and RECORDED (ADR-067). Chain verification finally crosses the installation boundary: `SysCore::install_verified_component` admits an ed25519 DIRECT root signature or an ENDORSED CHAIN (root endorses signer, signer signs the content hash) against PUBLIC keys only — never instantiating guest code. Admitted entities record the whole chain as evidence (kind, root, signer, component signature, endorsement, ABI version), and the launch gate RE-JUDGES that evidence against CURRENT trust at EVERY run: revoking a signer goes dark at the next launch for everything it ever signed, rotation after compromise without reinstalling anything, with the refusal audited by name (RevokedSigner / UnendorsedSigner / BadComponentSignature are three different responses). The same wave unified code admission across ALL install paths (`admit_wasm`) — including the signed path that had missed the ABI gate. Host proofs: `aletheia/tests/component_supply_chain.rs` (7 tests: chain install+run under secure policy both shapes, unendorsed refused by name, swapped-bytes refused via hash coverage, revocation LIVE against an already-admitted component, verified composition still works). Named non-claims: one chain shape only (root→signer→artifact), no expiry semantics, symmetric HMAC path remains accepted as ADR-025 Phase 1 legacy, hardware roots/anti-rollback stay REQ-BOOT-001 |
| ALET-P2-050 | P2 | resolved | FOUND BY BUILDING P1-023 and closed in the same wave: under secure policy the SPAWN path skipped provenance entirely — `prepare_spawn` loaded stored code straight into execution with no signature check, so an unsigned application that had slipped into the store was ONE spawn host call away from running, a side door around every signature check. Closed by generalizing the launch gate: `launch_provenance_ok` re-verifies stored provenance against CURRENT trust on BOTH paths (`run_installed` AND spawn), refusals audited with gate and app id; a smuggled child is now visibly refused in the audit log rather than silently swallowed |
| ALET-P1-024 | P1 | resolved | a dependency is pulled in only under AUTHORITY (ADR-068). `SPAWN_ACTION` existed and was audited - but never EVALUATED: naming an installed application queued it for execution unconditionally, making dependency resolution a naming convention instead of a boundary. Now the edge is checked at TWO layers: `host_spawn` evaluates `component.spawn` over THIS child against the parent's exact authority before queueing (Deny refused with the attempt audited, RequireApproval preserves the human gate at the component boundary), and `prepare_spawn` RE-EVALUATES against the current registry before loading any code, auditing refusals as ComponentSpawnDenied - the queue is a request, never a verdict. The grant is SCOPABLE in the existing lattice: component.spawn over Scope::Entities pins exactly which apps a component may ever pull in; revocation is live. Underneath, the older bounds still hold: children attenuated from parents, spawn depth bounded, cycles terminate. Host proofs: `aletheia/tests/component_dependencies.rs` (4 tests: no-authority refused by name with nothing queued; scoped grant resolves exactly its named dependency and no other; approval-constrained spawn does not compose inline; revoked grant resolves nothing live). Named non-claims: dependencies are resolved by run-time request - there is no separate install-time dependency manifest (the capability scope IS the declaration); per-parent quotas and dependency version pins beyond the ABI remain future work |
| ALET-P1-025 | P1 | resolved | `docs/INVARIANT-CONTRACTS.md` §INV-CAP-REVOKE states six invariants and `kernel-core/tests/cap_concurrency.rs` proves each: once `revoke` returns, 50 further attempts all deny and NO effect runs; revocation is permanent — a revoked parent cannot be delegated from, a fresh mint never reuses the id, and offering a revoked token beside a live one reports the LIVE one as the authorizing capability (no laundering); revoke is idempotent and a forged handle is a no-op; revoking a parent kills child and grandchild transitively (each proven to have worked first, so the test cannot pass vacuously); an interleaved revoke swept over every position inside a 6-step commit body yields a complete effect or a denial, never a partial; and revoking one sibling disturbs neither its siblings nor its parent |
| ALET-P1-026 | P1 | resolved | capability lifetime across a reboot (REQ-CAP-008, ADR-048, contract §INV-CAP-LIFE). ADR-038 made ENTITIES durable and stated plainly that authority is not — the engine is born empty at every boot, which is the safe default and a real limitation: an OS whose authority evaporates on restart has to re-mint wide authority at a point in the boot where nothing has authenticated anyone. `kernel-core/src/capstore.rs` is the model, and because persisting authority is the dangerous direction it is written as a list of REFUSALS: a persisted registry is untrusted input, and there is no partial load, because the parts a partial load drops (the revocation list, a parent record) are the parts that make the rest safe. `load` re-runs the SAME `capalg::attenuates` the engine applies at delegation time over every parent/child edge, so a store edited to widen a child is a whole-image refusal rather than a delegation that never passed `delegate`. Three refusals are the substance rather than hygiene: **ClockRewound** — expiry is relative to a logical clock, so reloading under a clock that restarts at zero un-expires everything, and the image therefore carries the clock it was saved under; **IdReusable** — ids come from `next_id ^ secret`, so an image whose counter could re-mint a stored id would hand a REVOKED token back to whoever still holds it, and the check proves the property directly rather than trusting the counter; and the **cascade is re-derived** from the verified parent edges rather than replayed from the list, so naming only a cascade's root — the smallest edit that resurrects the most authority — still comes back with the whole subtree dead. **Writing that last one found a real defect in the loader's first draft:** the re-derivation was `for r in already_revoked { revoke(r) }`, and `revoke` descends only when its `insert` reports the id as newly revoked, so every seed was already in the set, the walk stopped at the first node, and every descendant came back LIVE. Eleven invariants on all three targets (`[cap] ALL 11 CAPABILITY-LIFETIME INVARIANTS HOLD`, boot fails 110+i), six of them in the conformance contract; 19 host proofs in `kernel-core/tests/capstore.rs` including a per-byte per-bit corruption sweep, a per-prefix truncation sweep, and a ten-reboot campaign. **Named non-claims, both registered rather than implied:** the image is CHECKSUMMED, not AUTHENTICATED — the bit sweep proves the checksum covers every byte, and whoever can write the block can write a matching one, so signing it waits on ALET-P1-028 (key management); and nothing yet writes the image to a disk — `save`/`load` are the model and its admission test, proved in kernel space on every target, while committing it through `persist.rs` is a filesystem ordering decision (does the capability store commit in the same transaction as the entities whose authority it describes?) that deserves its own wave rather than a paragraph. The logical clock is still a caller-supplied constant on every target; monotonicity is ENFORCED, where a real monotonic value comes from is not solved |
| ALET-P1-027 | P1 | resolved | the authority lattice, written down once and proved by exhaustion (REQ-CAP-007, ADR-048, contract §INV-CAP-SCOPE). Every capability guarantee here rests on attenuation, and until this row closed the phrase 'equal or narrower' had no definition anywhere: `delegate` compared parent and child field by field, inline, with whichever predicate was at hand, and `docs/INVARIANT-CONTRACTS.md` had no section for it — the load-bearing property with the least written down about it. `kernel-core/src/capalg.rs` states three partial orders (action patterns, scopes, constraints) and their conjunction `attenuates`, applied by BOTH `spine::CapEngine::delegate` at mint time and `capstore::load` at admission time, because two copies of 'narrower' is two places for authority to widen. **Writing the relation as a function and sweeping it found a live privilege amplification.** An action pattern denotes a SET of actions, and two different questions are asked of patterns: `action_covers(pattern, action)` — is this concrete action inside the reach — which is what `evaluate` needs, and `action_attenuates(parent, child)` — is the child's REACH a subset of the parent's — which is what `delegate` needs. `delegate` was asking the FIRST question with the child's pattern in the action slot. The two agree on every pattern whose only `*` is trailing and disagree the moment one appears elsewhere: `entity.*.*` reaches `entity.*` as a string but not `entity.delete`, while `entity.*` reaches `entity.delete` — so the delegation was ACCEPTED and the child then authorized an action its parent never could, through the one mechanism the whole model says cannot amplify. Fixed in the kernel spine AND the hosted `aletheia/src/capabilities.rs`, because a component proved safe on one host and run on the other is not proved safe, and pinned as spine invariant 12 (with its legitimate-narrowing twin at 13) in the conformance contract on all three targets. Ten host proofs sweep the WHOLE finite universe the model can express — soundness against `evaluate`'s own covering functions over the complete scope lattice × target universe, reflexivity, transitivity, the conjunction agreeing with its parts in both directions — plus a 20 000-chain rejection campaign asserting no descendant of a legal chain exceeds its root, which fails if too few steps were accepted so it cannot pass vacuously. Two smaller corrections fell out: `Entities([])` and `None` are now recognized as the same scope (a relation that refused the narrowest possible delegation on a spelling pushes callers toward wider ones), and `Type(T) ↔ Entities([…])` is refused in BOTH directions with the reason recorded — a `Target` carries id and etype independently, so neither is a subset of the other, and deciding a particular case needs a store lookup an authority check must not acquire. **Deliberately incomplete, never unsound**, and the test proves the incompleteness is real rather than conservative. Not claimed: the lattice governs the three dimensions the model HAS; a future constraint dimension is a new order that must be added here and swept, which is precisely why the universe generators live beside the relation |
| ALET-P1-034 | P1 | resolved | capability image persistence is wired to the namespace; ALL FOUR halves now DELIVERED: custody+rotation (ADR-070: versioned keystore sealed with in-tree RFC 8439 ChaCha20-Poly1305, one-way rotation whose retirement DESTROYS the retired key, constructed prefix||counter nonces reserved before use, three-commit rekey pivot crash-proved at every recorded device-op position); SECURE-BOOT DELIVERY (ADR-072: the 32-byte root arrives over the firmware configuration channel - fw_cfg ioports on q35, MMIO windows on both virt machines - through ONE door (bootroot::open_custody) that names every impostor: RootNotProvided, FirmwareAbsent, MalformedRoot, KeystoreAuth, RolledBack; booted without the item the machine refuses BY NAME and continues, proved as a THIRD boot in all three QEMU gates; VirtualBox lists FirmwareAbsent honestly); and the COMBINED-TRANSACTION question DECIDED: image and entity store stay two commits, mutually detectable - each paired commit writes the vault generation INSIDE the durable entity record (under its trailing checksum) and every custody open enforces witnessed_generation <= keystore_counter, so rolling BOTH vault objects back together - ADR-070's pinned undetectable residual - is now caught BY NAME, with crash positions forward-safe by commit order and the host proof sweeping every device-op position. Evidence: kernel-core/src/bootroot.rs; kernel-core/src/fwcfg.rs; kernel-core/tests/bootroot.rs; per-target transports in kernel*/src/fwcfg.rs; [vault] ALL 14 CUSTODY-DELIVERY INVARIANTS HOLD on all three targets (gate-marker map vault=14, deliberate); third-boot absence refusals asserted by all three gates |
| ALET-P2-037 | P2 | resolved | two gate scripts were committed **non-executable**, and the check that exists to catch that could not. `scripts/vm-e2e-vbox.sh` and `scripts/vbox-install.sh` were mode `100644`, so the CI steps invoking `./scripts/vm-e2e-vbox.sh` died with `Permission denied` — taking the second-hypervisor job and, through it, the `ci-parity` job red on every push. `check-ci-parity.sh` DID assert executability, with `[ ! -x ]`; the reason it never fired is that the development clone lives on a Windows filesystem reached through WSL, where `/mnt/c` is drvfs and reports EVERY file as mode 777. The test was unconditionally true on the one host where the mode is most likely to be wrong. It now reads the mode from the **git index** (`git ls-files -s`, must be `100755`), because what CI executes is what git recorded, with the filesystem bit kept only as the fallback where git cannot answer. Same class as ALET-P2-035 (CRLF): a property of the developer's host silently weakening a gate, found only by looking at what the runner actually did |
| ALET-P2-038 | P2 | resolved | the ALET-P2-036 fix was correct for the development host and broke CI. On Ubuntu 26.04 the riscv64 emulator lives in `qemu-system-riscv`; on Ubuntu 24.04 — which is what `ubuntu-latest` still is — that package does not exist, and `apt-get install -y` on a package with no candidate exits non-zero. So three jobs (`vm-e2e-riscv`, `conformance`, `console-e2e`) died at their INSTALL step, before a line of Aletheia ran, on both pipelines. The optional package is now installed in its own grouped command — `… && (apt-get install -y qemu-system-riscv || true)`, parenthesized so only THAT install is optional and a failure of the required set still fails the job — and the gates keep the ALET-P2-036 behavior that actually matters: they preflight the emulator BY NAME and fail with that name, so an absent emulator is still a hard failure, never a silent pass. The lesson recorded rather than the patch: a package name is a property of a distribution release, and pinning one makes the pipeline depend on the developer's distribution |
| ALET-P2-039 | P2 | resolved | the OS you can sit in front of can now be typed at from the machine's own keyboard (REQ-CON-003, ADR-049, contract §INV-KEYMAP). REQ-CON-001/002 delivered an interactive console whose only input source was the UART, and the gates could not see it: `console-e2e.sh` types at the SERIAL LINE, and under `-serial stdio` or the VirtualBox host-pipe recipe the terminal IS the wire, so a kernel with no keyboard driver is indistinguishable from one with a working keyboard. It was found by a person booting the image on a VirtualBox GUI window, where the framebuffer showed the prompt and the keys reached nothing. **The keyboard is a second input SOURCE, not a second console:** `kernel_core::keymap` decodes scancode set 1 into bytes and pushes them into the SAME `conring` the UART feeds, so the line editor, its refusals and the overflow policy stay written once — two input paths with one editor, because two editors drift. The decoder's output alphabet is a security boundary (printable ASCII, CR, backspace, Ctrl-C — what the editor has rules for), proved over the ENTIRE input space: all 256 scancodes against every reachable modifier state, so no device can hand the editor a control byte it has no rule for. Ten decode invariants on ALL THREE targets, eight of them in the conformance contract. **The controller is enumerated, not poked:** `kernel-x86_64/src/ps2.rs` consults the ACPI FADT `IAPC_BOOT_ARCH` bit BEFORE touching a port (on a legacy-free platform those ports are unclaimed, and reading them is undefined rather than empty), distinguishes an ABSENT field from a ZERO one (absent means ACPI 1.0, where the controller is universal), runs the controller self-test and the port-1 interface test separately, rewrites the configuration byte after the self-test that resets it, resets and identifies the device, and READS THE CONFIG BACK — because the translation bit is what makes the set-1 decoder correct, and a controller that silently dropped that write would deliver set 2 into a set-1 decoder, presenting as a broken keymap rather than a broken assumption. Every wait is spin-bounded; a missing keyboard costs a bounded delay and a named reason, never a hang. Five bring-up invariants run on EVERY x86-64 boot including the non-interactive gate build — a driver that only runs when someone is sitting at the machine is a driver no gate covers — and the suite leaves IRQ1 MASKED, because arming an input source is the console's decision. `scripts/keyboard-e2e.sh` is the gate the old one could not be: the serial line is a FILE with no writer, and the operator drives the emulated i8042 through QMP `send-key`, so every keystroke travels controller → IRQ1 → PIC → vector 0x21 → decoder → ring → editor and the assertions are about Aletheia's own filesystem changing. Confirmed by hand on Oracle VirtualBox with `keyboardputstring` — the configuration the bug was reported from. The ACPI walk left `smp.rs` for `kernel-x86_64/src/acpi.rs` when the keyboard became its second consumer, and now verifies table checksums, which the MADT walk did not. Not claimed: no USB HID (legacy emulation covers most machines, a USB stack does not exist), one US QWERTY layout, no key repeat or LEDs, and aarch64/RISC-V still have serial input only because their QEMU `virt` machines expose no PS/2 controller |
| ALET-P2-040 | P2 | resolved | the console had a line BUFFER, not a line editor, and a cursor key could corrupt the line it was in the middle of (REQ-CON-004, ADR-050, contract §INV-CONSOLE-EDIT). ADR-044's editor was written as a fail-closed filter over single bytes: printable ASCII enters, everything else is discarded. That rule is correct about bytes and wrong about SEQUENCES. An arrow key is `ESC [ A`; the filter dropped the `ESC` (a byte it had no rule for) and then admitted `[` and `A`, because those are printable — so every arrow press typed `[A` into the middle of whatever was being written, and Home/End/Delete/PageUp/bracketed paste did the same with other letters. Reported as 'the input is messed up'; invisible to every gate in the repository for a structural reason worth recording — `console-e2e.sh` and `keyboard-e2e.sh` both typed CHARACTERS, and a gate that only presses letters can never find a bug in what happens when you press an arrow. The fix is a bounded three-state CSI parser in `kernel-core/src/shell.rs` (no byte inside a sequence can reach the line; an unrecognized final byte still closes the sequence, so the parser cannot be left armed to eat the next real keystroke; parameters are counted rather than buffered, so `ESC [` + 4096 digits costs eight bytes; a control byte inside a sequence abandons it and means what it says, so a stray `ESC [` cannot make the console ignore every later line), a real editor over it (cursor movement, mid-line insert/delete, kill-word/tail/line, bounded 32-entry history that restores the half-typed line a walk interrupted, Tab completion resolved by the SESSION against the live namespace), and one input alphabet — `shell::editor_accepts` — that `keymap` now delegates to rather than restating, so the decoder's exhaustive sweep proves 'every byte I can emit is one the editor has a rule for' against the editor's own definition instead of a private copy that can drift. The navigation keys emit the SAME sequences on both wires, and `ConsoleRing::push_seq` makes the sequence the ring's unit of admission, because a truncated arrow is worse than a dropped one. Measured: console invariants 15 → 30, input-ring 8 → 9, keyboard-decode 10 → 12 on all three targets; `keyboard-e2e.sh` 7 → 11 checks, four of which press keys that used to corrupt the line, asserting on Aletheia's own filesystem output rather than on the echo |
| ALET-P2-041 | P2 | resolved | the console had twelve commands and no way to work in it (REQ-CON-005, ADR-051, contract §INV-CONSOLE-CMD). Reported in the same breath as ALET-P2-040 — 'the console is too little as an OS' — and it is the same complaint: the console is the part of this system a person touches, and it was neither editable nor useful. There was no way to copy an object, rename one, add a line to one, look at one that is not text, search one, or find a name half-remembered; an operator keeping a note and a copy of it had to retype the note. Fifteen commands added under one rule — each must be a DIFFERENT path through machinery that already exists, and none may add a subsystem the console would then be the only proof of: `ver`, `lsblk`, `find`, `head`, `wc`, `grep`, `hexdump`, `append`, `touch`, `cp`, `mv`, `sync`, `history`, `clear`, `reboot`. Four needed a decision rather than an implementation, and each is written down because the alternative is a defect wearing a harmless name: `touch` NEVER truncates an object that exists; `mv` is copy-then-remove in that order and is NOT atomic (a crash leaves both names, which is recoverable — the other order loses the data); `append` is a read-modify-write through the single-transaction `replace`, so a crash leaves the old contents or the new ones; and a bad numeric argument is REFUSED rather than defaulted, because quietly reading ten lines makes the output an answer to a question nobody asked. `reboot` is the platform's own reset on all three targets (i8042 pulse / SBI SRST / PSCI SYSTEM_RESET over the conduit `smp.rs` already uses) and returns only on failure, where the console says the machine did not restart instead of hanging in a loop pretending it did. Measured: console invariants 30 → 40 on every target, six of them added to the cross-target conformance contract (112 → 118 named behaviors) because a target whose `mv` left both names would be a different operating system wearing the same command table; `console-e2e.sh` now drives the working set at a live console on all three targets and re-reads what `append`, `cp` and `mv` produced AFTER a reboot, so the effects are proved to be on the medium rather than in the session |
| ALET-P1-028 | P1 | resolved | key management as a LIFECYCLE, not a key file (ADR-069): data keys are VERSIONED entries in a keystore sealed under a ROOT-DERIVED subkey (HMAC-SHA256 domain-separated) so the root file never sits beside a ciphertext made under it; rotate mints max+1 with fresh key AND nonce prefix, writes use the newest, old frames stay readable while held; rekey rewrites the whole log under one version from an append-order journal mirror and retires the rest — atomic temp+rename; refusals named (retire-above-newest, stranding retirement, retired-version frame, keystore auth failure with NO partial load, corrupt root); files written 0600 via temp+rename. Host proofs: aletheia/tests/encryption_at_rest.rs (rotate/rekey/wrong-root/keystore-tamper). Non-claims unchanged: root custody and secure-boot delivery stay REQ-BOOT-001 |
| ALET-P1-029 | P1 | resolved | nonce lifecycle PROVEN per object (ADR-069): every DATA frame nonce is CONSTRUCTED — 32-bit per-key-version random prefix || 64-bit monotone counter — so reuse is impossible BY CONSTRUCTION rather than improbable by birthday bound; the counter ledger IS the log (high-water marks recovered from AUTHENTICATED frames at every reopen, forward-only); exhaustion at u64::MAX is a NAMED refusal demanding rotation, never a wraparound; the keystore object alone uses random nonces BY WRITTEN BOUND (~rewrites²/2^97 < 10^-17 for a million rewrites — bounded population, stated in the ADR). Host proofs: aletheia/tests/encryption_at_rest.rs (global distinctness + strict monotonicity across reopen cycles; named exhaustion), unit tests in aletheia/src/atrest.rs |
| ALET-P1-030 | P1 | resolved | encrypted content-addressing identity semantics DEFINED AND PROVED (ADR-069): the address remains SHA-256(PLAINTEXT) — identity/dedup/references outlive the storage encoding — while equal plaintexts produce DIFFERENT ciphertext frames (constructed per-frame nonces), so equality leaks nothing on the wire; BOTH halves proved, plus position-bound AEAD frames (AAD covers seq+len+key-version) refusing transposition/deletion/duplication/resend with the position named, an EXHAUSTIVE every-single-bit-flip sweep over a whole log image, torn-tail refusal, and boundary truncation pinned as the documented residual in both directions; legacy pre-ADR-069 logs are detected by trial-AUTHENTICATION and migrated wholesale before any append through the UNCHANGED public API. Contract: docs/INVARIANT-CONTRACTS.md §INV-ATREST (9 invariants). Host proofs: aletheia/tests/encryption_at_rest.rs (11) |
| ALET-P1-031 | P1 | resolved | x86-64 builds its OWN kernel map and RUNS ON IT (REQ-MM-006, ADR-034 + its 2026-08-03 addendum). `kernel-x86_64/src/kmap.rs` takes the image base/size from `LoadedImage` before `ExitBootServices` and the per-section permissions from the image's own PE section table — the x86-64 equivalent of the `linker.ld` symbols the other two targets read — then builds an identity map: 2 MiB RW+NX huge pages for RAM and sub-4 GiB MMIO, 4 KiB pages over every 2 MiB region the image touches (text RO+X, `.rodata` RO+NX, data/bss RW+NX, headers/padding RO+NX), every table frame claimed through the ADR-030 ownership model. Nine invariants prove each address class is mapped as the right thing BEFORE `kmap::activate()` writes CR3 (build-then-switch, so a wrong map fails a check instead of triple-faulting), and CR4.PGE is cycled across the write because OVMF marks its mappings global and a global TLB entry survives a CR3 load. Measured: 4 GiB covered, 2043 huge + 2560 page leaves, 5 split blocks, 11 table frames; **live audit 4603 leaves, 0 violations** where the inherited tree had 524 795 of 524 799 — and the spine, 4-core SMP and the entire ring-3 suite now execute on the kernel's tree. Boot fails closed (exit 28) on any live violation; the gate requires the ACTIVE marker + zero live violations instead of pinning a bootstrap count. Virtual-memory invariants 40 → 52. Not claimed: devices above 4 GiB (64-bit PCI BARs) are unmapped by this tree |
| ALET-P2-001 | P2 | resolved | dated Rust toolchain pin: `nightly-2026-08-09` (rustc 1.99.0-nightly, `771916f90`, LLVM 23.1.0) in all three bare-metal `rust-toolchain.toml` files and in **every** nightly-installing job of both pipelines — not only `quality`, because a job that installs a different nightly than the toml names makes rustup fetch a second toolchain and hides which one built the artifact. **Why it took two attempts, recorded because the register exists to prevent unverified claims:** the 2026-08-03 attempt was written and reverted after the install rolled back on the development workstation. The same class of failure reproduced on 2026-08-09 as a *partial* install — `rustup toolchain uninstall` left files behind (`os error 145: The directory is not empty`) and the next install failed with `Missing manifest in toolchain`, leaving a toolchain that `rustup toolchain list` reported as present and that could not compile anything. Removing the toolchain directory outright and reinstalling succeeded. `docs/TOOLCHAIN.md` records the trap and the six places a bump must touch. Verified before landing: the full gate set (`quality-gate.sh`, `build-all.sh`, all three QEMU boot gates, `conformance.sh`, `console-e2e.sh`, `vm-e2e-vbox.sh`) re-run against the pinned compiler |
| ALET-P1-032 | P1 | resolved | single-hypervisor qualification (REQ-QUAL-004, ADR-046). Every boot gate ran on QEMU, so the x86-64 claim was 'correct against QEMU' — an assumption the emulator and the kernel hold together is unreachable by more QEMU testing. `scripts/vm-e2e-vbox.sh` boots the SAME image under Oracle VirtualBox (its own EFI, its own ACPI tables, SATA/AHCI, no `isa-debug-exit`), judged by serial-log marker parity with the QEMU gate, with the four device families VirtualBox cannot emulate listed as SKIP and re-named in the summary. `kernel-x86_64/scripts/mkesp.py` (dependency-free GPT/FAT32 ESP writer, deterministic) is what makes the rung host-independent. **It found a real defect on its first run:** x86-64 vm invariant 48 required the frame pool's base to be a 2 MiB block, true under OVMF and false under VirtualBox's EFI (largest conventional region at `0x100000`, inside the split first block) — the invariant had encoded the firmware's memory map. Split into a security property (RW+NX at any granularity) and a structural one (probed outside every split span). Wired into both pipelines and `e2e-all.sh`; `check-ci-parity.sh` PASSes with it. Not claimed: this is a second EMULATION of the platform contract, not hardware — ADR-013's hardware rung is untouched |
| ALET-P1-033 | P1 | resolved | POSIX import at the Service API / IPC boundary (REQ-SVC-002, ADR-047). `aletheia/src/service.rs` imported `std::os::unix::net::{UnixListener, UnixStream}` at module scope — the one seam the SAD names as Aletheia-owned and as the future capability-named kernel endpoint. Two consequences, both real: the hosted Core did not COMPILE on a Windows host (`E0433: cannot find 'unix' in 'os'`), so the 'temporary development environment' was in fact a Unix development environment; and the boundary had no substitution point, because the transport type WAS the API type. `aletheia/src/transport.rs` owns a `Listener`/`Conn`/endpoint seam; `service.rs` now names no platform API on any host. Unix backend: unchanged behavior. Windows backend: loopback TCP on an ephemeral port behind a rendezvous file carrying a 32-byte `BCryptGenRandom` connect token, compared in constant time and demanded as the first frame before anything is parsed. The weaker posture of the Windows backend is stated in the ADR, not glossed. Proved on BOTH hosts: the conformance suite's endpoint round-trip is green on Linux and on Windows |
| ALET-P2-035 | P2 | resolved | executable text checked out with CRLF. The repository had no `.gitattributes`, so with Git's Windows default (`core.autocrlf=true`) every gate script arrived with CRLF and died as `env: 'bash\r': No such file or directory` before running a single check — not only on Windows but in any WSL, container or VM sharing that clone. Since ADR-046 makes a Windows workstation a first-class place to run the x86-64 qualification, executable text is now pinned to LF by `.gitattributes` (with binary fixtures — `.wasm`, `.efi`, `.img` — excluded from conversion, since a WASM module with its `0x0d` bytes rewritten fails validation far from the cause). Found while running the gates from a Windows clone, which is exactly the configuration the new rung exists to support |
| ALET-P2-036 | P2 | resolved | both pipelines installed the wrong QEMU package for RISC-V. On current Ubuntu the riscv64 system emulator moved out of `qemu-system-misc` into `qemu-system-riscv`; with it absent the gate died inside its perl watchdog printing `Died at -e line 1.` followed by 21 marker-missing lines, which reads as a broken kernel rather than a missing package. Both pipelines install both packages, and `vm-e2e.sh` / `vm-e2e-riscv.sh` now PREFLIGHT the emulator by name and fail with that name — the gate still FAILS (never a silent pass); what changed is that the diagnosis is legible |
| ALET-P2-002 | P2 | resolved | `--locked` enforced in `build-all.sh` + existing CI `--locked`; each crate carries `Cargo.lock` |
| ALET-P2-003 | P2 | resolved | `scripts/quality-gate.sh` (REQ-QUAL-002), run by a `quality` job in BOTH pipelines (which `check-ci-parity.sh` requires to match): `cargo fmt --check` and `clippy -D warnings` for all six crates — each linted from its own directory, because a bare-metal crate's own config (e.g. `kernel/.cargo/config.toml`) is what supplies its target and `build-std`, and passing `--target` from the root instead produces E0463 that reads like a broken lint run — plus advisories, licenses and SBOM. Writing it found four real defects in the same session's code: dead code in `pci.rs`, `.clone()` on a `Copy` capability token, mutable borrows where shared sufficed, and two crates whose formatting had drifted |
| ALET-P2-004 | P2 | resolved | `cargo audit` against the committed lockfiles, step [3] of `scripts/quality-gate.sh`; both pipelines `cargo install cargo-audit --locked` so the step RUNS in CI. On a host without the tool it prints an explicit SKIP and the summary names it again — the same never-a-silent-pass doctrine the VM gates use for an absent disk |
| ALET-P2-005 | P2 | resolved | `scripts/sbom.py` derives the inventory from `cargo metadata --locked` (no extra toolchain, consistent with ADR-004's pure-Rust posture): `build/sbom/<crate>.json` with every package's name, version, source and license, sorted and **timestamp-free** so an unchanged lockfile produces a byte-identical file — a timestamp would make every run a diff and hide the real ones. Both pipelines publish it as an artifact. A license allow-list (permissive only) fails CI on an unexpected license; it handles both the SPDX (`MIT OR Apache-2.0`) and legacy slash (`MIT/Apache-2.0`) forms, because the first draft rejected 11 permissive dependencies for their punctuation — the kind of false failure that gets a gate disabled. 117 packages inventoried |
| ALET-P2-006 | P2 | open | reproducible builds as a release property |
| ALET-P2-007 | P2 | resolved | strengthen marker-based VM testing (structured machine-readable markers). **DELIVERED (REQ-QUAL-005, ADR-061):** `scripts/lib-markers.sh` turns every boot log's "[tag] ALL N NAME INVARIANTS HOLD" sentences into a tag=count MAP, and each of the three VM gates holds that map against an expected family/count table declared IN THE GATE (measured per target: aarch64 ≡ RISC-V identical 19-family maps — the same arch-independent suites must prove the same counts over either bus; x86-64 differs by measurement: mm=22, vm=72, usermode=39, plus its own ps2=5). The per-family prose greps stay as named diagnoses; the map closes exactly what they cannot see: a family VANISHING from the boot, a count changing without the gate being told, and a new suite appearing unexamined — extra families are failures too, so joining the expected map is always deliberate. On success the gate prints one machine-readable line (GATE-MARKERS-V1: ...) CI can collect without parsing prose. Portable bash (no associative arrays — macOS ships 3.2), sorted-diff comparison names every difference in place, and the assertion was ATTACKED before shipping: wrong count, missing family, and unexpected family each verified to fail with a named diff |
| ALET-P2-008 | P2 | resolved | fault-injection coverage. **DELIVERED (REQ-QUAL-006, ADR-062):** `kernel-core/src/faultdev.rs` — `FaultInject<D>: BlockDevice`, a scripted adversary that pops an op program (Read/Write/Flush × Ok/Fail) and passes through once spent, with refusal semantics STATED AND PROVED: a refused write mutates nothing, a refused read leaves the caller's buffer unwritten (no zeroing fiction), a refused flush means the durability barrier did not hold and surfaces as Err(Device). `kernel-core/tests/faults.rs` drives it EXHAUSTIVELY through every position of the journal's commit protocol (a refusal at EACH of the 8 positions of a 2-update commit): after each refusal, recovery lands every home block in exactly one promised world — ALL-old or ALL-new, never mixed — with WHICH world decided by the pivot rule (pre-commit-record-flush ⇒ old and nothing replayed; post-pivot ⇒ new via replay). Recovery itself is swept the same way across all 6 positions of its own protocol (record read, payload reads, home writes, final flush), each ending in retry-completes-the-transaction — the idempotence claim is now load-bearing and tested. Boundary sweep proves the journal refuses to aim at reserved blocks and out-of-range edges BY NAME over the WHOLE index space |
| ALET-P2-009 | P2 | resolved | long-running soak testing. **DELIVERED (REQ-QUAL-007, ADR-063):** `kernel-core/src/soak.rs` runs FOUR lifecycle campaigns to a steady state, defined once for all three CPU targets and the host: journal transactions whose payloads are rewritten IN PLACE in buffers allocated before the meter starts; namespace create/replace/remove audited against the FULL structural contract after EVERY mutation with a fresh mount verifying exactly the survivors; capability grants churned share→attenuate→write→read→revoke with zero-copy observed every cycle, refcounts exact every cycle, and the refused paths (unauthorized, amplifying, revoked) attempted AND counted at volume; task generations over both arch-independent schedulers where a Finished task never dispatches again, Blocked tasks never dispatch, every priority drain is exactly-once, and unknown-id events change nothing. One fixed seed drives the load and the suite's final check RE-RUNS the campaign requiring identical checksums and censuses. The load-bearing claim is MEASURED, not assumed: on a target that meters its heap, journal churn allocates NOTHING per transaction — gated exactly, because the bump allocator never frees and a churn loop that grew per op would be counting down to death rather than enduring; `SparseDevice` materializes only touched blocks so the campaign's footprint is the WORK done, not the disk size. Throughput is reported per phase, never gated (QEMU-TCG nanoseconds are an emulator's — ADR-056's rule). Boot load (`BOOT_LOAD`) is sized against the tightest constraint, a TCG-emulated riscv64 inside a 120 s watchdog running the campaign TWICE; `kernel-core/tests/soak.rs` takes the same harness to tens of thousands of transactions, cycles and generations on a real allocator. Three arch-neutral behaviors join the conformance contract (126 → 129) and all three VM gates hold a new `soak=12` marker family (ADR-061 maps updated deliberately) |
| ALET-P2-010 | P2 | open | larger/more diverse property-test campaigns |
| ALET-P2-011 | P2 | resolved | this register + `check-traceability.sh` prevent manual-metric drift |
| ALET-P2-012 | P2 | resolved | the register now CHECKS ITSELF: `scripts/check-register.sh` (REQ-QUAL-003), run by a `register` job in both pipelines. Three drifts it refuses: (1) a `resolved` row citing a file that no longer exists — a claim whose evidence was deleted or moved; (2) a row citing a `REQ-` id the traceability matrix does not track; (3) **the rollup arithmetic disagreeing with the rows**, which is the one failure that would quietly invalidate every count this project reports. Wiring it found a real defect immediately: the ALET-P2-003 row cited a generic .cargo/config.toml path that exists per crate but not at the repo root. Together with `check-traceability.sh` (delivered ⇒ evidence exists) and `check-ci-parity.sh` (claimed gates actually run), all three claim surfaces are now machine-checked |
| ALET-P2-013 | P2 | resolved | `docs/MATURITY.md` states the distinction once and grades every subsystem against it: **P**roved (a gate fails when it breaks), **I**mplemented (works on the paths exercised; failure modes/scale/adversarial surface not covered), **A**rchitecture (written down, no code claimed), **X** production-ready. Its headline is that **nothing is X** — with the seven things production would additionally require named (a task supervisor, since a `KillTask` verdict currently ends the boot; DMA isolation; interrupt-driven I/O; real hardware variety; soak/fault injection at scale; key lifecycle and forgery-resistant integrity; update/rollback) — plus a "how to read a claim in this repo" section pointing at the four places claims live and their relative strength |
| ALET-P2-014 | P2 | deferred | complete production boot chain — milestone work |
| ALET-P2-015 | P2 | deferred | secure boot — milestone work (ADR needed) |
| ALET-P2-016 | P2 | deferred | update/rollback system — milestone work |
| ALET-P2-017 | P2 | deferred | recovery architecture — milestone work |
| ALET-P2-018 | P2 | resolved | the storage stack has a top: a named-object filesystem namespace over the journal (REQ-FS-001, ADR-035). `kernel-core/src/fs.rs` keeps a directory block + an allocation bitmap block as ordinary home blocks, and commits every mutation — the data blocks, the bitmap and the directory — as ONE journal transaction, so a name and its allocation can never disagree and there is no repair pass because there is no inconsistent state to repair. A removed object's blocks are zeroed inside the same transaction (erase on delete, the storage twin of ADR-033), and the transaction bound is refused (`TooLarge`), never truncated. Proved on the host by `kernel-core/tests/fs.rs` — a crash sweep over EVERY prefix of a create and of a remove (each outcome must be the whole object or none of it, with an unrelated object untouched), a 4 000-op campaign re-checking the structural invariants after every op, and every refusal asserted to be a no-op — and in-kernel by 12 named behaviors in all three VM gates, which `conformance.sh` now requires of every target (37 → 49 core behaviors). On aarch64 the SAME twelve also run over the real virtio-blk device through the virtqueue (virtio invariants 5 → 17), so crash atomicity is a claim about hardware. Not claimed: one flat namespace, one bitmap block, contiguous extents only, no rename/in-place update, no per-object integrity beyond the commit checksum, no encryption at rest (ALET-P1-028/029 remain open). **Follow-on delivered (REQ-STOR-003, ADR-038):** the namespace gained an ATOMIC UPDATE (`Filesystem::replace` — one transaction carrying the new blocks, the old blocks zeroed, the bitmap and the directory, because "remove then create" would lose the NAME at a crash between them), and on top of it `kernel-core/src/persist.rs` makes the spine store durable: whole-store save through `replace`, and a load that re-verifies each entity's content address AND a trailing record checksum. The checksum exists because the byte-flip sweep in `kernel-core/tests/persist.rs` proved a real hole — flipping an entity's *id* loaded successfully with different data, since the content hash covers only content. Nine behaviors per target (`ALL 9 DURABLE-STORE INVARIANTS HOLD`, boot fails 200+i), fs suite 12 → 15, real-device suite 17 → 20, conformance 49 → 61. A corrupt store is REFUSED, never silently reformatted. Capabilities are deliberately not persisted — that is ALET-P1-026, still open. **Proved by REBOOT:** each gate attaches a second, persistent disk (`probe_nth` / `find_virtio_blk_nth`) and boots the same kernel TWICE — boot 1 must create the store, boot 2 must load and re-verify boot 1's entity through the real driver (`PERSISTENT MEDIUM: boot #2, 1 entities verified`), and boot 1's line is in the conformance contract (62 behaviors). On x86-64 the NVRAM copy is per-boot while the disks are shared, so the second run is the same machine restarting |
| ALET-P2-019 | P2 | deferred | complete driver model — milestone work. **Progress, not closure (REQ-DRV-004, ADR-036):** the virtio-blk driver moved out of the aarch64 *dev* crate into `kernel-core/src/virtioblk.rs` behind a two-function per-target seam (`VirtioHal`: an identity-mapped DMA frame + a barrier) plus an `MmioLayout`, so RISC-V — a **first-class** target that had no real storage at all — now drives a real disk and proves the journal AND the whole 12-behavior filesystem namespace over the virtqueue (`ALL 17 VIRTIO-BLK INVARIANTS HOLD` on both targets, boot fails 120+i / 180+i; host-proved in `kernel-core/tests/virtioblk.rs`, which pins the count, the numbering and the group order and asserts a mis-sized device is refused at invariant 2 before any I/O). Still missing for the *model*: hotplug, interrupt-driven completion (the poll is synchronous, one request in flight), multi-queue, device restart/recovery, DMA isolation (ALET-P1-018 — the device gets raw physical addresses), and — until REQ-DRV-005 — x86-64. **Second step (REQ-DRV-005, ADR-037):** the BUS became a seam too (`Transport`, with `MmioTransport` in kernel-core and `kernel-x86_64/src/pci.rs` for virtio-pci: legacy-port config space, bus-0 scan, capability walk for COMMON/NOTIFY/DEVICE cfg, exactly two command bits), so x86-64 now drives a real disk as well — all three targets prove the journal AND the 12-behavior filesystem namespace over hardware paths. QEMU places the BAR above 4 GiB, outside the kernel's own map, so the driver maps its own registers through a NEW admission rule whose physical test is INVERTED: `AddrPlan::validate_map_device` refuses RAM the allocator owns (`MapFault::PhysIsRam`) exactly where `validate_map` requires it, so the device path can never alias a task's frame as MMIO (host sweep in `kernel-core/tests/vmaddr.rs` proves no page is mappable as both, on all three plans; x86-64 vm invariants 52 → 55 prove the live API refuses a RAM range). Still missing for the *model*: hotplug, interrupt-driven completion, multi-queue, restart/recovery, PCI bridge recursion, BAR assignment, and DMA isolation — ALET-P1-018, now MORE concrete because bus-master is enabled |
| ALET-P2-020 | P2 | open | networking stack — milestone work. **First slice DELIVERED (REQ-NET-001/002, ADR-041):** a virtio-net driver on ALL THREE targets over both buses, on a new reusable multi-queue substrate (`kernel-core/src/virtq.rs` — a block device's single fixed ring cannot serve a NIC, which needs rx+tx and buffers posted BEFORE `DRIVER_OK` because a packet arrives whether the driver is ready or not), plus enough protocol to prove the path against something that ANSWERS: ARP resolves QEMU's gateway, and an ICMP echo with two correct checksums comes back with matching id, sequence and payload (a wrong checksum is dropped in silence, so the reply proves the packet was well FORMED). A second echo must match on its own sequence, so the driver reads the reply rather than assuming. Four behaviors in the conformance contract (64 → 68). Fixing it exposed a real seam bug: `PciTransport::identity()` returned the PCI device id where the shared driver expects the VIRTIO device kind, so a good NIC was refused. **Second slice DELIVERED (REQ-NET-003, ADR-060):** an ARP cache (`kernel-core/src/arpcache.rs` — fixed capacity 8, byte-exact keys, refresh-in-place, LRU by monotonic tick, no allocation on the resolve path) whose whole point is OBSERVABLE: `arp_wire_requests()` counts broadcasts actually sent and the suite resolves twice requiring the counter to stay at 1 — a cache that "worked" while the wire saw a second request would be a cache in name only. A UDP datagram service (`udpv4.rs`) over ONE internet checksum for the whole stack (virtionet re-exports it so ICMP and UDP cannot drift into two definitions of well-formed), verified at BOTH ends: the builder writes IPv4+UDP with both checksums correct; the parser refuses short buffers, lying header and total lengths, a broken header checksum, length fields that lie in either direction, ZERO ("sender did not compute") checksums REFUSED BY NAME, and a pseudo-header checksum under which a datagram re-addressed IN FLIGHT fails verification instead of parsing — the host suite proves exhaustively that EVERY single-bit flip anywhere in a received datagram is refused by some checksum, and every truncation at every prefix. DHCP DISCOVER→OFFER (`dhcp.rs`) — the machine ASKS the network where it lives: a bounds-checked option walk honoring PAD and END and skipping unknowns by declared length, the transaction id BOUND (an answer to someone else's question is not an answer to ours), message type required to be OFFER, an offer of no address refused after it proves to be an offer; the OFFER is EVIDENCE and is never taken (no REQUEST, no ACK, no renewal). The slice's headline invariant: **the address the network offers IS the address the driver claims** — the constant is now cross-checked against the authority that assigns it, identically on all three targets. Four new conformance behaviors (122 → 126); the network suite grows 5 → 9 (`ALL 9 NETWORK INVARIANTS HOLD`, boot failing 220+i). **Still missing for the STACK:** TCP, routing, fragmentation, a socket layer, interrupt-driven completion — every reply is still matched synchronously by one waiting caller.
| ALET-P2-021 | P2 | open | graphics/compositor — milestone work. **First slice DELIVERED (REQ-GFX-001, ADR-057):** a virtio-gpu driver on ALL THREE targets over both buses (virtio-mmio on aarch64/RISC-V, virtio-pci on x86-64) on the shared substrate, which gained TWO-descriptor CHAINS (`Virtqueue::add_chain` — request + device-writable response; the first live boot found the missing `F_NEXT` on the request half as QEMU writing ZERO response bytes, recorded at the flag). The 13-invariant suite proves, against the real device: init to DRIVER_OK with every optional feature DECLINED; a GET_DISPLAY_INFO round trip whose table is structurally valid and whose head geometry is PINNED to measurement (1280x800 — not the 1024x768 the author expected from spec recall; the pin says so at the site); twelve-plus-four invalid requests refused by NAME with silence MEASURED by a device-traffic counter; the full 2D resource lifecycle (CREATE_2D 256x64, ATTACH_BACKING of sixteen DMA-gated pages, SET_SCANOUT, TRANSFER_TO_HOST_2D, RESOURCE_FLUSH, DETACH_BACKING, UNREF) each answered OK; and the lifecycle END proven by the DEVICE's own error grammar — a flush for a never-created id and the SAME flush after unref both draw ERR_INVALID_RESOURCE_ID from the device itself (an echo would say OK). Host tests pin the wire layout field-for-field against the UAPI header and the fail-closed display-info parser. `ALL 13 VIRTIO-GPU INVARIANTS HOLD`, boot failing `301 + i`, required by all three VM gates; VirtualBox lists the family SKIP (no virtio-gpu). **Second slice DELIVERED (REQ-GFX-002, ADR-058):** the framebuffer console — `fbcon::Surface` maps pixels across 150 NON-contiguous single frames (scatter-gather is the device's OWN shape), a public-domain 8x8 font double-struck to 8x16 cells, a cursor/wrap/scroll state machine whose control bytes are fail-closed like the serial editor's, pixel-exact host proofs against the font table itself, and a live suite that renders "Aletheia OS" into REAL backing pages, TRANSFER+FLUSHes the whole frame, and proves DETACH REVOKES every page's DMA registration (the registry counter returns to exactly ring + two buffers — first wave's stated limitation, closed). `ALL 6 FRAMEBUFFER-CONSOLE INVARIANTS HOLD` on all three gates. **Third slice DELIVERED (ADR-077):** the COMPOSITION CONTRACT is modeled, not assumed — `kernel-core/src/compositor.rs` gives every surface a possession-based owner token that gates every op (`NotOwner` refused by name), clips placements exactly to the scanout (fully-off refused at attach and move; the write loops only visit pixels existing in both surface and scanout, proved against a guard-band raster), keeps the painter's order as the owner-controlled z-order, refuses size-dishonest packed fills with nothing touched, makes placement changes visible the same frame through bounded coalescing screen-space damage (clear-then-repaint through the z-order — no ghosts, no over-painting the windows above), writes ZERO pixels on an unchanged frame with every frame's cost REPORTED (`FrameStats`), and lands bit-identical under identical sequences: 14 boot-gate invariants on all three targets (`compositor=14`, boot fails 600+i), six behaviors pinned cross-CPU in the conformance contract, 11 host proofs in `kernel-core/tests/compositor.rs`. **Fourth slice DELIVERED (ADR-078):** the composition contract MEETS THE SCANOUT — `fbcon::ComposeSink` implements the compositor's `Raster` over the framebuffer console's scatter-gather backing frames (the same page list a virtio-gpu 2D resource names as its backing store) and COUNTS every put and refusal, so "the model's bound and the real raster's bounds agree" is a measured zero; `virtiogpu::compose_suite` drives the real device on all three targets — resource created, 150 DMA-gated pages attached, scanout bound, the first compose read back pixel-exact and flushed as exactly TWO commands, a quiet frame writing ZERO pixels and issuing ZERO device commands (measured on the driver's command counter), a wrong token changing no real byte and no traffic, a move visible the same frame with no ghost, the z-order flipping on real pixels, a 160-px overhang landing only its intersection with the sink never asked outside, and the teardown revoking every page with the DEVICE confirming `ERR_INVALID_RESOURCE_ID`: 8 boot invariants (`compose=8`, boot fails 640+i), two cross-CPU conformance behaviors, 5 host proofs in `tests/compfb.rs`. **Fifth slice DELIVERED (ADR-079):** the composed desktop MEETS INPUT — input routing and the cursor plane: exactly ONE possession-based input session gates every event post, focus change and cursor move (`InputSealed` on a second opener, absent/wrong/forged tokens all `NotInputSession`, refused by name and counted); at most ONE focused surface, placed-only (`NotPlaced`/`UnknownSurface` by name), with the loser told `FocusLost` through its own bounded queue (dropped AND counted if full) and an idempotent refocus queueing nothing; `post_key` routes a decoded keymap-alphabet keystroke to the focused surface's `MAX_INPUT_EVENTS`-bounded queue and `drain_input` empties it OWNER TOKEN ONLY — the input path decides WHERE events go, the owner decides WHO reads them, neither can act as the other, delivery is seq-monotonic and exactly-once, a no-focus keystroke is refused `NoFocus` and exists nowhere, overflow is refused `Backlogged` and counted without evicting the backlog, draining restores capacity exactly, detaching the focused surface clears focus and the queue dies with the surface (a re-minted id starts empty, the old token is dead); the cursor is the compositor's OWN plane — not a surface, no token, no z-order slot — an 8x8 transparent crosshair only the session may move or hide, fully-off refused `CursorOffScanout` with the position named, partially-off clipped exactly (guard-band proofs through every edge), painted LAST above every surface as a mask (window ink shows through transparent bits, raising changes nothing), visible the SAME frame, free when it does not move, its cost REPORTED in the new `FrameStats::cursor_pixels`; and input is not pixels — a keystroke with no repaint writes nothing, identical input sequences land bit-identical: 12 boot invariants on all three targets (`input=12`, boot fails 660+i, unconditional — VirtualBox proves the family too), four cross-CPU conformance behaviors (154 -> 158), 8 host proofs in `tests/input.rs`. **The input HARDWARE rung is DELIVERED (REQ-GFX-005, ADR-080):** `kernel-core/src/vinput.rs` is one virtio-input driver over both buses with the transports' first config WRITE named as the `ConfigWrite` seam, devices classified by their OWN declared names (lone/unclassifiable refused by name), a Linux-keycode decoder whose whole output space is the console's alphabet (host-swept over every keycode in every modifier state; grammar shared with the PS/2 decoder, modifier state NOT), an absolute-axis pointer decoder committing on SYN with the device's own pinned range mapped exactly onto the scanout, and the click as a ROUTING decision (`Compositor::focus_at`: topmost placed surface under the point, empty click clears, loser told, forged session refused, autorepeat ignored); `kernel-x86_64/src/desktop.rs` installs the LIVE desktop before the VT-d gate and pumps it from the PIT tick — bounded, compose-only-on-change, one TRANSFER + one FLUSH per changed frame, one writer, facts as single machine words the new `input` console command reads: 10 boot invariants against the REAL devices on all three targets (`vinput=10`, init fails 679, suite fails 680+i; `input_suite` 12 -> 13), seven cross-CPU conformance behaviors (158 -> 165), 10 + 2 host proofs (`tests/vinput.rs`, `tests/input.rs`), and the LIVE gate `scripts/vinput-e2e.sh` driving the pump with QMP-injected real device events (cursor at the axis-mapped position, click focuses / empty click clears, one virtio keystroke posting exactly one byte queued behind the focused window while the console sees nothing, quiet counters still). Unsafe audit CHANGED and counted (+14/+1/+12/+1). **Still missing for the GRAPHICS STACK, stated:** a live desktop pump on aarch64/RISC-V (the driver, decoder and routing are proved against their real devices in the boot suite; only x86-64 installs the timer-pumped desktop), interrupt-driven input (the pump polls the used rings from the tick), the PS/2 wire through the session (i8042 bytes still feed the serial console only), relative pointing devices and hotplug (the unplugged flag is read at bring-up), device-level GPU isolation between surfaces (QEMU's virtio-gpu still enforces nothing about who composes — the enforcement lives in the kernel-side contract layered over the DMA registry), interrupt-driven completion (one command pair per changed frame is synchronous), alpha blending beyond the 1-bit depth fbcon runs, and text rendering/IME inside surfaces |
| ALET-P2-022 | P2 | open | power management. **The CONTRACT rung is DELIVERED (ADR-076):** `kernel-core/src/pm.rs` defines the full enforcement semantics as a software model — frequency domains with honest discrete ladders (registration refuses dishonest ones by name), the governor range free to ANY caller, the overclock band gated by live per-domain elevation grants that attenuate on delegation (`Amplification`/`CrossDomain` refused), revoke with cascade and an IMMEDIATE clamp of the domain back to nominal, the thermal envelope absolute BY CONSTRUCTION (no ladder point above it can register, no grant past it can mint), a thermal trip clamping EVERY domain with a tick-exact cooldown that refuses even valid grants, a demand governor that never enters the OC band and never parks demanded silicon, idle residency + wake latency accounted exactly across interfering transitions (a clock change closes the span and books it — real time is never lost), device power along legal arcs only (D3→D1 refused — wake through D0), and every accepted act and refusal recorded in a bounded audit ledger under a monotonic sequence with the holder named on grant acts. 14 boot-gate invariants on all three targets (`[pm] ALL 14 POWER-PERFORMANCE INVARIANTS HOLD`, boot fails 560+i), six pinned cross-CPU in the conformance contract, 19 host proofs in `kernel-core/tests/pm.rs` (the full OC-band decision table, 5^3 attenuation-chain sweep, cooldown tick-exactness, envelope absoluteness, ledger wraparound, bit-identical determinism). **Still open:** the hardware rung — MSR/CPPC/ACPI frequency programming into real silicon (QEMU TCG exposes no frequency control to the guest, so a rung attempted today could only prove code ran, not that anything enforced — the ADR-071 posture), battery, system sleep/wake (S3), voltage rail enforcement beyond recording mV in the ladder, thermodynamic temperature simulation (callers report; the contract decides) |
| ALET-P2-023 | P2 | deferred | hardware discovery (ACPI/DT) completion — milestone work |
| ALET-P2-024 | P2 | deferred | native AI runtime as a complete OS subsystem — milestone work |
| ALET-P2-025 | P2 | open | context lifecycle resource boundaries |
| ALET-P2-026 | P2 | resolved | erase-on-free (REQ-MM-005, ADR-033): every target zeroes a frame at release — the choke point explicit frees, reclamation and teardown all share — so no frame carries a previous owner's bytes. Proved by the reuse case (pattern → free → same frame back → zeros) in each VM gate (memory invariants 17 → 21, x86-64 22) + the erase behavior added to the conformance core contract. Not claimed: frames never owned still hold firmware bytes (pre-boot memory, not task data) |
| ALET-P2-027 | P2 | open | relationship-graph access capability enforcement |
| ALET-P2-028 | P2 | open | intent-confusion attack testing |
| ALET-P2-029 | P2 | open | continuous threat-model maintenance |
| ALET-P2-030 | P2 | open | explicit security-boundary enumeration |
| ALET-P2-031 | P2 | open | DoS ≠ unauthorized-access distinction in the threat model |
| ALET-P2-032 | P2 | resolved | the kernel-image mapping refusal lives ONCE, in `kernel-core/src/vmaddr.rs`: `AddrPlan::with_protected` + `MapFault::ProtectedVirt` (REQ-MM-006, ADR-034 addendum). Closed at exactly the moment the row predicted — ALET-P1-031 gave x86-64 a split and would have made `in_image_split` a third copy — so instead the two existing copies were deleted and all three targets now declare only WHERE their image is (`image_split_span()` on aarch64/RISC-V, `kmap::protected_span()` on x86-64), while the refusal itself is one implementation applied by both `validate_map` and `validate_unmap`. Host-proved page by page across the span in `kernel-core/tests/vmaddr.rs`: both APIs refuse every page, the boundaries are half-open, a malformed protected address still reports the malformation first, and an undeclared span protects nothing. The two refusal behaviors joined the `conformance.sh` core contract (37 named behaviors) now that all three targets emit them in identical words |
| ALET-P2-033 | P2 | resolved | every DERIVED address space has the dead pages dead, and it is a property of construction rather than of order (REQ-MM-007/008, ADR-040 follow-on, contract §INV-DEADVA). `kernel-core/src/deadva.rs` states the rule once: a target DECLARES its dead spans, `audit` walks them in ANY root through caller-supplied translation **and** leaf lookups, and a report is clean only if it walked something — an EMPTY declaration is itself a violation, so a target that forgets cannot pass vacuously. Both questions are asked because a page can be unreachable and still be covered by a live block descriptor, one split away from being revived; that is the exact shape this row recorded. Wired fail-closed into all three builders (`build_space` on x86-64, `build_identity` on aarch64/RISC-V): a tree that fails the audit yields NO space and returns its frames, because handing out a space that can reach the guard is worse than failing to build one. Six invariants host-proved in `kernel-core/src/deadva.rs`'s own tests, four per target in the live gates (virtual-memory 62 → 66 on aarch64/RISC-V, 66 → 71 on x86-64), and four behaviors added to the `conformance.sh` core contract (74 → 78, all three targets PASS). The fail-closed path is ATTACKED, not asserted: `build_space_from(active_root())` runs before `kmap::activate()`, when `active_root()` really is OVMF's dirty tree, and the x86-64 invariant requires the refusal AND an unchanged frame count — a builder that leaked its two table frames on the refusal would turn fail-closed into exhaustion. Scoped honestly: `build_identity` on aarch64/RISC-V takes no source, so nothing can hand it a failing tree, and §INV-DEADVA-6 says x86-64 only rather than claiming three targets from one test. The design change is written down in ADR-040's 2026-08-07 addendum. **Writing it found the real defect the row only suspected:** `build_space` copied whatever CR3 held, and `kmap::activate()` runs AFTER the virtual-memory suite — so a space built during it copied OVMF's tree, which maps VA 0 as RAM and covers the ring-0 stack guard with a 2 MiB huge page. The audit turned that into a hard boot failure instead of an inherited hole; the fix is `vm::space_source_root()`, which derives from the kernel's own map whenever one exists. That change also corrected two neighbouring invariants that had encoded the OLD source: the teardown suite searched PML4 slots 1..512 for a shared kernel table, which only ever matched because OVMF's tree spread across them — the kernel's own map covers 4 GiB entirely inside PML4[0], so the sharing a teardown must not disturb lives one level down, in the private PDPT |
| ALET-P2-034 | P2 | resolved | `kmap` records the pages it deliberately left unmapped and the boot suite ASSERTS the count: x86-64 invariant 63 requires exactly two (the ring-0 stack guard and VA 0). The absence of those leaves was already proved by walking the live tree; this closes the other half — a refactor that stopped skipping, while something else happened to leave a leaf absent, would have passed |
| ALET-P2-042 | P2 | resolved | the resident model was a pair of `const`s and a manifest nothing read, so changing the OS's intelligence required a rebuild and the machine could not say what it was running (REQ-AI-004, ADR-052). Now the catalog is **discovered**: `aletheia/src/ai/registry.rs` scans the local model cache (honoring `HF_HUB_CACHE`/`HF_HOME`) and reports what is really on the machine, including models no manifest names — marked `unpinned`, because an uncharacterized model is one whose parameters are guesses. `models/lfm2.5.toml`, `models/minicpm.toml` and `models/aletheia-lm.toml` CHARACTERIZE models rather than enumerating them, carrying only what a directory listing cannot: the pinned checksum, measured sampling, the forced-`<think>` flag, and the structured-output strategy. The first cut of this row was itself the defect it closed — a hardcoded set of three manifests compiled in with `include_str!`, which is a guess about somebody else's disk wearing the word "registry" — and was rewritten to scan. Selection persists, a unique prefix selects, an ambiguous one refuses, and the pinned checksum is now VERIFIED on demand by `aletheia/src/ai/runtime.rs` (`verified` / `MISMATCH` / `not pinned` / `unreadable` as four distinct outcomes, streamed so a gigabyte artifact costs no resident memory). LFM2.5-2.6B is the new default (sha256 and size MEASURED, not copied from a listing); MiniCPM is retained rather than deleted so earlier STATUS measurements keep a baseline; and Aletheia's own model is registered BEFORE its weights exist, so selecting it reports `NOT YET TRAINED` instead of silently serving something else. Two adjacent defects fell out of writing it: `resolve_model_path` picked the LARGEST cached `.gguf`, which hands the provider a different quant than the one whose checksum was pinned, and the model commands would have written their selection into `data_dir`'s per-run temp directory — a switch that reports success and has no effect. Proved in `aletheia/tests/model_registry.rs` plus the module's own unit tests |
| ALET-P2-043 | P2 | resolved | three defects in the AI path that only a LIVE model could expose, all found by the first run of `aletheia/src/ai/bench.rs` (REQ-AI-005, ADR-052). (a) `aletheia/src/ai/llama.rs` took only the first resolved address: `localhost` resolves to `::1` before `127.0.0.1` and `llama-server` binds IPv4 only, so every health probe failed and the Core fell back to the deterministic interpreter *while the model was running* — live since ADR-017 and indistinguishable from "no model present", which is why nothing caught it. (b) The GBNF grammar was written for one model: LFM2.5's chat template opens with a token the grammar has no rule for, so generation died at token zero and returned an EMPTY completion for all six operations; the structured-output strategy is now per-manifest. (c) `n_predict = 512` truncated a schema-constrained decode mid-whitespace on the widest argument list, returning empty with `finish_reason: length` — a truncation that presents as "the model cannot plan this operation". Measured on this workstation: 0/6, then 2/6, then **6/6 on two consecutive runs**, against a deterministic control arm at 6/6 and 0 ms. The benchmark refuses to record any number until the backend's advertised model id matches the selected manifest, because the endpoint is a port and another project's service already held `:8080` on the machine this was written on |
| ALET-P2-044 | P2 | resolved | the kernel console had no path to the resident model, and `bench.rs` said so in its own output: two command surfaces, intelligence attached to one of them (REQ-AI-006, ADR-053). Closed by a SECOND operation family derived from `kernel_core::shell::COMMANDS` — the table the dispatcher and `help` are generated from, depended on rather than retyped, so a command cannot be added to the kernel without appearing in the model's menu. The hosted side adds what the kernel table cannot know: an exhaustive risk classification (anything that writes to the medium or stops the machine is `Destructive`, and a command with no explicit arm fails a test), and a rendering contract in which a validated step becomes EXACTLY ONE console line. No inference engine entered kernel space: `kernel-core` is still `no_std` with no network, the model runs on the host, and what crosses into the guest is a line of ASCII indistinguishable from one a person typed |
| ALET-P2-045 | P2 | resolved | five defects the first live console-planning runs exposed, four of them in the request protocol and none of them findable without a model (REQ-AI-006, ADR-053). (a) A permissive `args` object let the model loop keys — `{"name":"manifesto","text":"manifesto","op":"cat","name":…}` — and burn the whole generation budget on three of eight cases, returning empty; per-command exact shapes removed the failure rather than rejecting it downstream. (b) Caught in that raw output, the model was trying to escape into `<|tool_call_start|>[write(name='notes', …)]`: it had produced the right call and the decode would not let it emit the format it was trained on. The console path now uses NATIVE TOOL CALLING, and `runtime::spawn_llama_server` passes `--jinja`, without which `llama-server` never parses a tool call and a correct answer reads as no answer at all. (c) A prohibition naming a command — *"only call `ls` when…"* — took the score from 6/8 to 3/8, because the negation is not what survives, the token is; the system prompt now names no command and a test enforces it. (d) Without a context brief the model answered `ls` for anything needing a file, its own reasoning saying *"Let me first look at what files are available"* — correct for an agent, wrong for an interpreter, and fixed by letting it see (ADR-018's lesson on a second surface). (e) A single brief for a whole benchmark run was stale by the last case, so the model planned `find notes` for "remove notes" — the benchmark contradicting itself, not the model failing. Measured on this workstation: 4/8 → 3/8 → 4/8 → 3/8 → 5-6/8 → **8/8 on two consecutive runs**, median ~800 ms, against a deterministic control arm at 8/8 and 0 ms |
| ALET-P2-046 | P2 | resolved | ADR-059: the console asks before it destroys. A destructive console line now goes through the Core's REAL pending-approval surface instead of a CLI flag: `request_console_approval` records a durable Pending bound to the EXACT rendered line (idempotent per subject+line, verdict from THE PolicyEngine so future policy changes apply without a second implementation); a human answers through the new `aletheiad approvals list|grant|deny` CLI; and `take_console_approval` runs in the driver AT TYPING TIME, spending the grant Granted→Consumed atomically with an ApprovalConsumed event that replay cannot resurrect — one yes buys one typed line. Binding was attacked at the gate: a granted 'rm poem' does not let 'rm manifesto' through; a denial is terminal for its record while a fresh ask opens a NEW question; the spend survives restart. The three operations are LOCAL-operator authority by explicit statement (aletheiad already owns its data dir) and are deliberately NOT exposed over the service boundary, where they would hand untrusted clients self-authorization; Verb::Console intents are refused by the pipeline rather than guessed into execution. Exit codes 0/1/7 keep stdout meaning only 'type this'. Host proofs: policy.rs spend-once state machine, tests/console_approvals.rs (5 tests: durability, idempotence, exact binding, denial, consumption-across-restart, two-worlds-do-not-blur). Gate: console-ai-e2e.sh replaced refuse-only with the full dance per arm (ask 7 → Pending listed → deny → fresh ask → grant → typed once → Consumed → spent types nothing), all records in a per-arm scratch data dir because grading governance in ~/.aletheia would inherit stale answers. **Follow-on delivered in the same wave:** the AGENT loop now consults a `Governor` seam — an unapproved session's destructive proposal ASKS (exit 7; the question lives IN the transcript), a grant types once at resume, a denial is a Correction while a second insistence stays terminal (`denied_lines`), an unreachable store refuses by name, and `--approve` remains inline consent that records nothing; `console-agent-e2e.sh` bound 1 replaced refuse-only with the full dance. Remaining non-claims: no expiry-by-time-travel test (TTL inherited unchanged); no kernel-side changes (hosted side only, consistent with ADR-053) |
| ALET-P2-047 | P2 | resolved | both halves closed (REQ-AI-007, ADR-054). **Multi-step:** `aletheia/src/ai/agent.rs` makes one request a bounded session — propose, validate, render, type, observe, propose again — in which the model reads the machine's answer to its own previous command. That deleted the `case "$planned" in write*|rm*|…` list of verbs in `console-ai-e2e.sh`, which was a second copy of the kernel's command table living in the one language nothing here tests, and incomplete in the way such a list always eventually is. Every ADR-053 guarantee is re-applied PER STEP, because a loop is only as safe as its weakest iteration, and three bounds exist only because it is a loop: a step budget, no-progress detection, and a refusal to end the session it is observing (`halt` refused even WITH approval — an agent that stops the machine cannot read the result of stopping it). Observations are admitted as data and bounded in Rust next to the tests rather than by whatever the driver's shell captured. **Three targets:** `scripts/console-agent-e2e.sh` is three legs — aarch64, riscv64, and x86-64 under OVMF — and the model arm now runs on all of them. Measured, this workstation (LFM2.5-2.6B-Q4_K_M, llama.cpp `--jinja`, `-c 8192`, 27 tools offered): **all six arms PASS** — 3/3 cases on each of three CPU targets driven by a real model at a live console, plus the four bound assertions and the power-cycle leg on each |
| ALET-P2-048 | P2 | resolved | four defects the first live AGENT session found, and none of them were the model (REQ-AI-008/009/010, ADR-055). The loop passed its deterministic arm on the first attempt and then failed **all three** cases against a real model — the third consecutive wave in which that sentence is the finding, which makes it a design rule rather than an anecdote. (a) **A refusal the model was never shown.** Asked to show one line of an object it proposed `cat` with a two-word name; `console_ops` refused correctly and `advance` turned that into the end of the session. Aletheia knew exactly what was wrong — *"cat: name must be one word"* — and said it to nobody, so a mistake the model would have fixed in one sentence killed a session instead. ADR-054's own enum comment claimed *"every reason here is a reason the next attempt would be identical"*, which was false for most of them; `Refusal::is_recoverable` had already been written, documented as *"the whole safety content of the agent loop's retry"*, and was never called. Recoverable refusals are now `Correction`s that enter the transcript the model reads, bounded at 3 and NOT charged to the console budget — a refused proposal types nothing, and charging it would make the budget mean two things at once. The split is MALFORMING versus OVERREACHING: missing authority, `halt`, an exhausted budget and an unreachable backend stay terminal, because asking again changes nothing except how many times the model was invited to try. (b) **No-progress assumed the machine never moves.** The model ran `cat poem`, `append poem second line`, `cat poem` — and the third step was refused as "already in the transcript". It was, and it said `hello world!`, because it ran BEFORE the append; Aletheia refused the one command that answered the request. That is precisely the defect ADR-054 accuses the old shell list of, in Rust. Repetition is now judged over the turns since the machine last changed, using the `Destructive` classification already derived from `kernel_core::shell::COMMANDS` — no second list — and the rule is deliberately conservative about staleness rather than precise about dependency. (c) **The first cut of that fix was itself the next defect:** with a window that reset on every mutation the model ran `cat`, `append`, `cat`, `append`, `append`, each append resetting the window that would have caught the next one, while `poem` grew from 25 bytes to 49. A repeated MUTATION is never progress — it teaches nothing and, unlike a repeated read, leaves damage behind — so the window is for readings only. (d) **The gate asserted a preference and called it a claim.** Asked how big a copy was, the model ran `cp manifesto backup` then `stat backup` — correct, and reached through exactly the dependency the case exists to assert — and the gate failed it for not being `wc backup`. `AgentCase::must_type` is now a list of `Reached { line, console_says }` pairs, index-aligned across the TSV the shell reads, and one alternative must be BOTH typed at the live console and confirmed by what the console printed; checking the two independently would be a much weaker claim and an invisibly weaker one in a passing log. Also in this row: a whole-transcript byte bound that elides the oldest output visibly (per-reply bounds do not bound a session, and six turns of 2 KB against an 8192-TOKEN window is how a loop silently loses its own brief), and per-turn wall time plus model-call counts reported so the gate prints a median rather than a claim |
| ALET-P3-001 | P3 | resolved | centralized assembly/Rust boundary documentation: docs/ASM-BOUNDARY.md inventories every asm!/naked_asm!/global_asm! site (27 files / 93 sites) with per-file counts and the reason each family exists; scripts/check-boundary-docs.sh regenerates the inventory FROM THE TREE and fails on an undocumented site OR a stale row, and a CI job runs it every push |
| ALET-P3-002 | P3 | resolved | unsafe audit ownership written down and machine-checked: docs/UNSAFE-AUDIT.md inventories the token-exact unsafe surface of all six crates under a STATED counting rule (occurrences on code lines), names the owner set and the other-than-author review rule, requires a // SAFETY: invariant argument per block (445 present) and confinement to hardware-boundary leaf modules; the checker holds every count exactly so growth is visible in review by construction |
| ALET-P3-003 | P3 | resolved | centralized architectural invariants index: docs/INVARIANTS-INDEX.md maps all 18 INV-* contract families to their requirement ids, what each pins, and its proof surface (host property suite / boot-gate marker families / conformance behavior / e2e script); check-boundary-docs.sh enforces SET PARITY between contract sections and index rows in BOTH directions, run by CI every push |
| ALET-P3-004 | P2 | resolved | `aletheia_risk` Low verdict rate is 8.11 × 10⁻⁷ — the three-way verdict is operationally a two-way {Elevated, Abstain}. Doc: `docs/gap/MLRISK-OVERLOAD-AUDIT.md` §DEFECT-1 (1 B-advice measurement on the installed blob). Owner: `kernel-core/src/mlrisk.rs` (`Advice` carries no signal for "model had no opinion vs model decided Low"); the fix is either a documented two-way regime or an additional `Advice` field |
| ALET-P3-005 | P2 | resolved | the audit's premise was FALSE and is withdrawn: the contract constrains exactly five features (`job_fails`, `user_fails`, `user_fail_rate`, `cell_fails_prev`, `cell_evicts_prev`, each `+1`) — not nine; the "9 of 20" figure was this document's own misattribution, MODEL-CARD never states a count. A rigorous tree-walk of the shipped blob (every split on a constrained feature must have all left-subtree leaves <= all right-subtree leaves) finds ZERO violations across all 171 trees, matching the boundary sweep's five monotone-ascending features exactly. The real gap the row pointed at — nothing re-asserted any of it between training and shipping — is closed: `export.py::assert_monotone` now hard-fails before `write_blob` on any crossing split, per-feature results recorded in `export_kernel.json`. Doc: `docs/gap/MLRISK-OVERLOAD-AUDIT.md` §DEFECT-2 (corrected). Owner: `aletheia-ml/src/aletheia_ml/export.py` |
| ALET-P3-006 | P2 | resolved | all-zero feature vector is a constant margin `-1 781 991` and a guaranteed `Elevated` verdict on every call; the kernel treats this as a decisive opinion when it is a degenerate input. Doc: `docs/gap/MLRISK-OVERLOAD-AUDIT.md` §DEFECT-3 (50 M fault-inject + 250 M all-zero surface). Owner: `kernel-core/src/mlrisk.rs::advise` — add a third abstention cause (`degenerate`) alongside `out_of_range` and the conformal band |
| ALET-P3-007 | P0 | resolved | `PriorityScheduler` drain is O(N²) via `self.order.retain` in `schedule_next` and `finish`; 200 K tasks admits in 6 s but does not drain in 349 s of wall time. Gate bench (8 K tasks) finishes the drain but does not detect the non-linear cost growth. Doc: `docs/gap/MLRISK-OVERLOAD-AUDIT.md` §DEFECT-4. Owner: `kernel-core/src/priosched.rs` — replace `VecDeque<TaskId>` + `retain` with a structure that supports pop-min-priority and arbitrary-remove in sub-linear time (BTreeSet keyed on `(effective_priority, TaskId)`), and extend the gate with a 200 K-task drain case so this never ships unfixed again |
| ALET-P3-008 | P2 | resolved | inverted conformal band still ships (MODEL-CARD §4): `abstain_lo > abstain_hi` → band-fires=0 at every scale from 2 M to 1 B advices. The trainer-side `check_band` (MODEL-CARD §4) catches it at training but `kernel-core/src/mlrisk.rs::load` does NOT refuse an inverted-band blob, so the kernel loads and runs an inferentially-evaluable-but-never-band-firing model silently. Doc: `docs/gap/MLRISK-OVERLOAD-AUDIT.md` §DEFECT-5. Owner: `kernel-core/src/mlrisk.rs::load` (add `ModelError::InvertedBand` and refuse at boot); `kernel-core/src/mlrisk_stress.rs` (add a boot-time refusal test) |
| ALET-P3-009 | P3 | resolved | durable objects were stored RAW: every byte the store image, capability exports and journal payloads needed at rest was charged to the device, and no codec existed anywhere in the tree to change that. Closed with `kernel-core/src/compress.rs` — an integer-only LZSS codec (4 KiB window, one storage block) in a self-describing `ACMP1` envelope carrying the DECLARED original length (decoding is bounded before allocation, capped at `MAX_OUTPUT`, so a corrupt or hostile envelope cannot become a decompression bomb), a checksum over the original bytes, and a RAW fallback so compression never makes an object bigger. Wired into the boot path: `persist::save_compressed`/`load_compressed` detect envelope vs raw record (an image written by an older kernel still loads — detection, not assumption), `open_and_witness` now writes compressed images, and the durable-store suite grew 9 → 10 invariants (compressed round-trip costs fewer blocks AND the raw path stays readable). Capability exports are covered too: all four `capstore::*_to_fs` paths seal their images in the same envelope, loads verify the RAW image by its own magic before accepting it as raw (a damaged envelope is named as container damage, not misread as a bad image), and the MAC still authenticates the pre-compression image. Hosted gates: `kernel-core/tests/compress.rs` proves exact round-trip over empty/tiny/repetitive/overlapping-run/noise inputs, per-field corruption refusals, determinism, and the allocation bound |


**Rollup (2026-09-02, forty-fifth update):** 96 findings — **77 resolved**, 12 open, 7 deferred. (This wave ADVANCED **ALET-P2-021** to its input HARDWARE rung — ADR-080: real devices through the input session, and the desktop goes LIVE. One virtio-input driver over both buses on all three targets with the transports' first config WRITE as a named seam; devices classified by their own names; a keycode decoder whose whole output space is the console's alphabet; an absolute-axis pointer decoder steering the cursor plane with the device's own pinned range mapped exactly; the click as a routing decision (`focus_at`); the x86-64 LIVE desktop pumped from the PIT tick, bounded, compose-only-on-change, one writer, facts as machine words behind the new `input` command: 10 boot invariants against the real devices on all three targets (vinput=10; input_suite 12 -> 13), seven cross-CPU conformance behaviors (158 -> 165), 10 + 2 host proofs, the LIVE gate `scripts/vinput-e2e.sh` driving the pump with QMP-injected events, unsafe audit CHANGED and counted (+14/+1/+12/+1). Still open in the row: a live pump on aarch64/RISC-V, interrupt-driven input, PS/2 through the session, relative pointers, hotplug, device-level GPU isolation, alpha, text/IME.)

**Rollup (2026-08-29, forty-fourth update):** 96 findings — **77 resolved**, 12 open, 7 deferred. (This wave ADVANCED **ALET-P2-021** again, to its input-routing rung — ADR-079: the composed desktop meets input. The input path is ONE possession-based session gating every event post, focus change and cursor move; at most ONE placed surface is focused and the loser is told FocusLost through its own bounded queue; keystrokes route to the focused surface only and drain only through its OWNER token — the input path decides WHERE events go, the owner decides WHO reads them; bounded queues refuse Backlogged by name and count drops, capacity restores exactly, detach kills queue and focus, re-mints start empty; the cursor is the compositor's OWN plane — session-moved only, fully-off refused by name, partially-off clipped exactly, painted above every surface as a transparent mask, its cost REPORTED in FrameStats::cursor_pixels; and input is not pixels — a keystroke writes nothing, identical input sequences land bit-identical: 12 boot invariants on all three targets (input=12, unconditional — VirtualBox proves the family), four cross-CPU conformance behaviors (154 -> 158), 8 host proofs. The input HARDWARE rung (real PS/2/serial bytes through the session) stays open in the row. The previous wave the day before advanced the same row to its real-pixel rung — ADR-078: the composition contract meets the scanout. `fbcon::ComposeSink` routes the model's sink at REAL scatter-gather backing frames with put/refusal counters that measure the model/raster bound agreement; `virtiogpu::compose_suite` creates the resource, attaches 150 DMA-gated pages, binds the scanout, flushes composed frames as exactly two device commands, proves a quiet frame moves nothing (zero writes, zero commands, measured on the driver's counter), a wrong token changes no real byte, moves are same-frame and ghost-free, the z-order flips on real pixels, overhangs clip exactly, and the teardown revokes every page with the device confirming the end: 8 boot invariants on all three targets (compose=8), two cross-CPU conformance behaviors (152 -> 154), 5 host proofs. Device-level GPU isolation stays open in the row — QEMU's virtio-gpu enforces nothing about who composes.) (The previous wave the same day ADVANCED **ALET-P2-021** in place — ADR-077: the composition contract is modeled, not assumed; pixels are authority (possession-based owner tokens gating every op) and the scanout is a hard bound (exact clipping proved against a guard-band raster, size-honest buffers, bounded coalescing damage, a zero-write quiet frame with every frame's cost reported, bit-identical determinism): 14 boot invariants on all three targets (compositor=14), six cross-CPU conformance behaviors, 11 host proofs.) (Before that the same day, **ALET-P2-022** left the deferred column with its contract rung delivered — ADR-076: the power/performance contract is modeled, not assumed. Frequency is AUTHORITY, heat is a HARD CEILING: every domain gets an honest discrete ladder, the governor range is free, the overclock band is gated by live per-domain elevation grants that attenuate on delegation and clamp back to nominal on revocation, the thermal envelope is absolute by construction, trips clamp every domain with a tick-exact cooldown, the demand governor never overclocks nor parks demanded silicon, idle residency and wake latency are accounted exactly, device power moves along legal arcs only, and every act lands in a bounded monotonic audit ledger. 14 boot invariants on all three targets (pm=14), six behaviors pinned cross-CPU, 19 host proofs. Hardware realization (MSR/CPPC/ACPI), battery, and S3 stay open in the row — QEMU TCG exposes no frequency control.) (Before that: the 2026-08-26 wave per-device DMA windows, ADR-075.)
This wave advances **ALET-P1-018** to its second hardware rung (ADR-074): the SMMUv3 on the
aarch64 virt target, discovered through the machine's own device tree delivered over the
firmware configuration channel, programmed stage-2-only through the shared contract seams,
enforced latched from the gate to halt - gated by a 10-invariant live suite (smmu=10) plus
15 host proofs against a simulated unit and a device-side walker built from the emulator's own
decoder shapes. The virtio-pci transport moved ONCE into kernel-core when this wave became its
second consumer (x86-64 deleted, not forked). Named honestly at the boundary: CLI-attached
virtio-pci DMA does not traverse the legacy iommu=smmuv3 unit on QEMU 11.1 (abort-canary
measured), so grant-serves/revocation-events stay open beside ADR-073's completion-loss
artifact. Also in-wave: kernel is its own PCI firmware (BAR sizing + assignment) because
bare-metal boots run none.

**Rollup (2026-08-24, fortieth update):** 96 findings — **77 resolved**, 11 open, 8 deferred. (This wave closed **ALET-P1-034** completely — ADR-072: the custody anchor crosses the platform boundary. DELIVERY: the vault root arrives over the firmware configuration channel through one named door, with RootNotProvided / FirmwareAbsent / MalformedRoot refused before any byte is decrypted, absence proved live by a third rootless boot in every QEMU gate, and VirtualBox reporting FirmwareAbsent honestly; DECISION: capability image and entity store remain two commits but are now MUTUALLY DETECTABLE — paired commits write the vault generation inside the durable entity-store record under its trailing checksum, custody-open enforces witnessed_generation <= keystore_counter, and the host proof constructs ADR-070's pinned undetectable attack (both vault objects rolled back together) and watches the witness catch it by name; before that: ADR-071 modeled the IOMMU contract and ADR-070 resolved the custody and rotation halves of this same row.) (This wave also closed ALET-P1-014 — scheduler multicore contention DEPTH: dynamic producer/consumer storms holding exactly-once for 4000 tasks, affinity respected under steal storms with mixed masks, empty-mask refusal pinned as changing nothing, and the ADR-028 lock-tripwire counters asserted zero after every storm via a test-only accessor; before that: ADR-070 resolved the custody and rotation halves of ALET-P1-034 — authority custody is a LIFECYCLE: a root-derived versioned keystore sealed with in-tree RFC 8439 ChaCha20-Poly1305, one-way rotation whose retirement destroys keys, constructed nonces reserved before use because the kernel has no boot entropy, a three-commit rekey pivot crash-proved at every recorded device-op position; it also closed ALET-P1-019 — virtio-blk production-grade depth: full completion validation refusing short reads before they touch the caller buffer, an idempotent reset/recovery path, and a lying host device-model fuzzing the malformation matrix; before that: ADR-069 resolved ALET-P1-028, P1-029 and P1-030 — encryption at rest is a LIFECYCLE, not a key file: versioned data keys under a root-derived keystore with rotation/rekey/retirement and named refusals, constructed prefix||counter nonces whose ledger is the authenticated log itself with exhaustion named rather than wrapped, position-bound AEAD frames refusing transposition/deletion/duplication/resend, exhaustive single-bit-flip refusal, plaintext-SHA-256 identity semantics proved in both directions, and legacy logs detected by trial-authentication and migrated wholesale through the unchanged public API; the same wave closed ALET-P3-001/002/003 — the assembly boundary, unsafe audit and invariant index are centralized documents machine-checked against the tree by scripts/check-boundary-docs.sh on every push; before that: ADR-068 resolved ALET-P1-024 — dependency resolution is now a real security boundary: spawn edges are capability-checked at the guest boundary AND re-evaluated by the host at fulfilment, scoped grants pin exactly which apps a component may pull in, approval-constrained and revoked grants refuse inline; before that: ADR-067 resolved ALET-P1-023 — the component supply chain is verified, live, and recorded: chain verification crosses the installation boundary, admitted entities record their full provenance evidence, the launch gate re-judges evidence against CURRENT trust so signer revocation goes live at the next launch, and all faults are named per link; it also opened and closed ALET-P2-050 — the spawn path had skipped provenance entirely under secure policy, a side door now closed by the same live gate; before that: ADR-066 resolved ALET-P1-022 — the component ABI is explicitly versioned: a custom-section declaration enforced at install and run, named refusals, SDK auto-stamping verified end-to-end, and the v1 import surface pinned by a live link probe; it also opened and closed ALET-P2-049 — the example-component build script now pins BOTH cargo and rustc via rustup, whose PATH-shadowed foreign rustc lacked wasm32 std and failed E0463 far from the cause; before that: ADR-065 resolved ALET-P1-021 — the WASM sandbox is bounded in every dimension beyond fuel: memory bytes, table elements, stack depth/height, and a wall-clock deadline enforced at host-call crossings, with every kill NAMED in the outcome and the audit event; before that: ADR-063 resolved ALET-P2-009 — long-running soak: four lifecycle campaigns under repetition with the allocation-free churn window held exactly on each target's own heap meter; ADR-061 resolved ALET-P2-007 — every VM gate now holds the boot's family/count marker map against an expected table and prints a GATE-MARKERS-V1 line; ADR-062 resolved ALET-P2-008 — scripted fault injection through every position of the journal's commit and recovery protocols.)

**ALET-P3-009** opens and closes the same day: files at rest were raw, and nothing in the tree
could change that. The compression layer is bounded by construction — declared length before
allocation, checksum after reconstruction, RAW fallback before waste — and it entered through the
boot path itself, so the VM gate's cross-reboot proof exercises it on every run.

**ALET-P3-005** closes the overload-audit set — by WITHDRAWING its premise and closing the real gap
behind it. The audit claimed four of nine monotone constraints were silently dropped at export; the
contract constrains five, the tree-walk finds zero violations, and the "nine" was this document's
own misattribution. What the row got right was the seam: nothing between training and shipping
re-asserted the property, so `export.py` now does, as a hard gate. A defect report that dissolves
under measurement is not a failure of the process that produced it — the bench was built to be
checked, the check was run, the claim lost. That is the register working.

Four of the five overload-audit defects closed in this wave (**ALET-P3-004**, **ALET-P3-006**,
**ALET-P3-007**, **ALET-P3-008**); **ALET-P3-005** stays open at the trainer boundary. The shape
each fix took applies the register's own lesson to itself: the P0 scheduler drain became an ordered
ready pool gated at the 200 K that found it — and that gate then caught the fix's OWN first draft
reintroducing a quadratic through the advisory tiebreak, which is the argument for gating at the N
that found the defect rather than a convenient one. The degenerate-input backdoor became a third
named abstention cause carried through every census from `advise` to the console line. The inverted
band was refused at kernel load AND re-made live at the trainer (alpha 0.10 → 0.03, measured by a
committed sweep script), because a refusal with no fixed artifact would have booted every machine
model-free and called that safety.

The console-agent wave closes **ALET-P2-047** in both halves and opens-and-closes **ALET-P2-048**.
The first was the honest remainder of the previous wave: a planning path that emitted one command per
request, driven on one CPU target out of three. It now emits a bounded SESSION — the model reads the
machine's answer to its own previous command — and the model arm runs on all three targets, six arms
PASS. The second is four runtime defects that only a live model could expose, and the shape repeats
the ADR-052 and ADR-053 lesson a third time: **every one presented as the model being incapable.** It
proposed a malformed command and died on a refusal Aletheia knew the words for and told nobody; it
re-read an object after changing it and was refused for "no progress" that was no longer true; the
first cut of that fix let it repeat the CHANGE instead, growing an object from 25 bytes to 49; and it
answered a question correctly with `stat` and was failed by a gate that wanted `wc`. Four fixes, none
of them in the model: say what was wrong and ask again, judge repetition against a machine that
moves, never repeat a mutation, and assert a claim rather than a preference.

What remains open in this area is **ALET-P2-046** — approval is still a CLI flag rather than the
Core's human-in-the-loop surface — and it is stated here rather than folded into a row that closed.

**Rollup (2026-08-17, twenty-second update):** 93 findings — **52 resolved**, 32 open, 9 deferred.

The five new rows are the resident-model overload wave (**ALET-P3-004** … **ALET-P3-008**), each
documented in `docs/gap/MLRISK-OVERLOAD-AUDIT.md`. The bench that surfaced them —
`kernel-core/examples/overload_bench.rs`, added in this wave — runs the installed
`aletheia_risk.altm` through 1 B advices, 20 M deterministic repeats, 50 M pathological inputs and a
5 M-sample-per-feature boundary sweep, all from a single host-side binary outside the test harness.
The lesson shape is a fifth iteration of the ADR-052/053/055 one: **defects only a measurement at
scale can expose, every one of them presenting as the model or the scheduler behaving correctly when
inspected at a small N**. (a) The three-way verdict is operationally a two-way {Elevated, Abstain}:
across 1 B uniform in-box advices, Low fired **811** times — 8.11 × 10⁻⁷ — and code reading the
verdict has no signal for "the model had no opinion" vs "the model decided Low" (ALET-P3-004). (b)
Of the 9 monotone-constrained features the trainer was told to enforce, only 5 are actually
monotone-ascending in the integer export; 4 are violated. The constraint is given to XGBoost at
training but not re-asserted on the blob (ALET-P3-005). (c) An all-zero feature vector is a constant
margin of `-1 781 991` and a guaranteed `Elevated` verdict on every call — a degenerate input the
kernel treats as a decisive opinion (ALET-P3-006). (d) `PriorityScheduler` drain is O(N²) via
`order.retain`; 200 K tasks admits in 6 s but does not drain in 349 s of wall time, while the gate's
8 K-task case finishes in 2 s. Same class of non-linear cost growth the MODEL-CARD §8 allocator fix
hid by making the inner loop cheap at N=128 — it hid it again, at a different seam (ALET-P3-007).
(e) The inverted conformal band (MODEL-CARD §4) is re-confirmed at every scale from 2 M to 1 B; the
trainer-side `check_band` catches it at training, but `kernel-core/src/mlrisk.rs::load` does not
refuse an inverted-band blob, so the kernel loads it and runs it silently (ALET-P3-008). The fix
plan is named in the audit doc; the verification plan is to re-run the bench at the same parameters
after each fix lands.

**Previous rollup (2026-08-10, twenty-first update):** 88 findings — **52 resolved**, 27 open, 9 deferred.

The four new rows are the console-planning wave (**ALET-P2-044**, **ALET-P2-045** closed;
**ALET-P2-046**, **ALET-P2-047** opened). The first is the hole the previous wave's own benchmark
printed in its output: two command surfaces, intelligence attached to one of them. The second is
five runtime defects in the request protocol that only a live model could expose, and the shape of
them is worth naming because it repeats the ADR-052 lesson at a higher level — *every one presented
as the model being incapable*. It looped JSON keys until it exhausted the budget and returned empty;
it tried to escape a constrained decode into the tool-call format it was actually trained on; it
answered `ls` to everything after a prompt tried to forbid `ls`; and it planned `find notes` for
"remove notes" because the benchmark's own stated context said `notes` did not exist. Four fixes,
none of them in the model: a channel it could speak, a prompt naming no command, a brief read off
the live machine, and a per-case context that does not go stale. The two rows OPENED are the honest
remainder rather than paragraphs inside the rows that closed: approval is still a CLI flag rather
than the Core's human-in-the-loop surface (**ALET-P2-046**), and the live gate drives one CPU target
out of three (**ALET-P2-047**).

**Previous rollup (2026-08-10, nineteenth update):** 83 findings — 48 resolved, 26 open, 9 deferred.
The
two new rows (**ALET-P2-042**, **ALET-P2-043**) were both opened and closed in the AI wave, and they
are worth separating. The first is a design defect the audit could have found by reading: the model
was a constant in the source, so an OS that calls intelligence a first-class subsystem could only
change it with a compiler. The second is a cluster of three runtime defects that **no amount of
reading would have found**, because each one presents as the model being absent or incapable rather
than as anything being wrong: a probe that only tried the first resolved address (so a running model
looked like no model at all, and had since ADR-017), a grammar written for one model's chat template
that silently produced empty output under another's, and an output budget that truncated a
schema-constrained decode into an empty completion. All three were exposed by the same thing —
actually running the resident model against the operation surface and checking the answers — which
is the argument for `aletheiad model bench` existing at all. Previous rollup (2026-08-09,
eighteenth update): 81 findings — 46 resolved, 26 open, 9 deferred. **ALET-P2-039 closed in the same session it opened**, which is worth saying plainly: it was reported by the first person to boot the image who was not its author, and no gate in this repository could have found it, because every console gate types at the serial line and under `-serial stdio` the terminal is the wire. The fix is a real i8042 driver — ACPI-declared, self-tested, spin-bounded, config-verified — and a gate that types on the emulated KEYBOARD rather than on the wire. Three of the new rows come from looking at what the RUNNER and the USER did rather than at what passed here: two gate scripts committed non-executable with the executability check defeated by drvfs (ALET-P2-037), a QEMU package name pinned to the developer's distribution release (ALET-P2-038), and — reported by the first person to boot the image who was not its author — an interactive console that cannot be typed at from the machine's own screen, because its only input source is the UART (ALET-P2-039). The new row is **ALET-P1-034**, the follow-on this wave created: the capability image exists and its admission test is proved, and nothing writes it to a disk yet — which is a filesystem transaction-ordering decision, not a capability one, and belongs in the backlog rather than in a paragraph of the row that closed. This wave closed the two capability rows that were the same question asked at two timescales — **ALET-P1-027** (what 'narrower' means) and **ALET-P1-026** (how long a capability lives) — because the answer to the second is an application of the first: what makes persisting authority safe is being able to re-run the admission test on the way back in, and that test is the lattice. Both found real defects rather than merely documenting working code: a privilege amplification in `delegate` that had been live since the engine was written, and a revocation cascade in the loader's first draft that came back empty. The register
gained four rows this wave (ALET-P1-032/033, ALET-P2-035/036) for defects the audit had not seen because they
only surface off the development host: a single-hypervisor qualification story, a POSIX import at the Service
boundary, CRLF-mangled gate scripts, and a CI package that no longer ships the RISC-V emulator. Three
previously-open rows closed (ALET-P1-009, ALET-P1-011, ALET-P2-001). The register
The newest close is **ALET-P2-033**, and it is the memory cluster's last follow-on: the dead pages
(VA 0, the ring-0 stack guard) are now dead in EVERY address space rather than only in the map each
kernel built for itself, stated once in `kernel-core/src/deadva.rs` and enforced fail-closed in all
three space builders. It also closed a defect the row had only suspected — `build_space` copied
whatever CR3 held, which before `kmap::activate()` is OVMF's tree, so a derived space really did
inherit a guard region mapped as one 2 MiB huge page. Two neighbouring invariants that had silently
encoded that old source were corrected in the same commit rather than left passing for the wrong
reason.

The register
is now machine-checked by `scripts/check-register.sh` (ALET-P2-012), including THIS arithmetic: a rollup
that disagrees with the rows fails CI, because a drifting anti-drift line would quietly invalidate every
count here. ALET-P2-020
(networking) moved from `deferred` to `open`: its first slice is real, gated code on all three targets
(ADR-041), and what remains — TCP/UDP/DHCP/routing/sockets — is named in the row rather than the whole
subsystem sitting as architecture text. The two new rows are follow-ons this session's work CREATED and would
otherwise have lived only in prose: derived address spaces do not inherit the guard page or the dead null
page, and the guard-page count the x86-64 builder records is never asserted.
ALET-P2-013 (`docs/MATURITY.md`) is the newest: every gate here answers "does it behave as specified",
none answers "is it ready for someone else's machine, data and adversaries", and conflating those is how a
project claims more than it has. Nothing in Aletheia is production-ready, and that file says so.
The newest two are the layout cluster (ALET-P1-006/012), and writing their check found a live hole
nothing else had: VA 0 was mapped by every boot identity map.
The newest three are the CI quality cluster (ALET-P2-003/004/005): the boot gates proved the OS behaves,
and nothing proved the SOURCE was in the state the project claims. ALET-P2-001 (dated toolchain pin) was
attempted in the same wave and left OPEN rather than claimed — see its row.
The trap-entry cluster closed two rows (ALET-P1-010 re-entrancy, ALET-P1-013 fault classification) and
REFINED two rather than flipping them on a partial claim: ALET-P1-009 keeps its `fuzz` half and
ALET-P1-011 keeps adversarial entry testing, each with the remaining work named in its row.
The newest four are the *written-contract* cluster — ALET-P1-005 / P1-016 / P1-017 / P1-025 — which
existed as working code with no statement of what must never happen. `docs/INVARIANT-CONTRACTS.md` is
that statement: 25 numbered invariants across TLB shootdown, priority inheritance, IPC cancellation and
capability revocation, each naming the host test that adversarially attempts to violate it.
The newest close is **ALET-P2-018**, the first of the deferred milestone subsystems to become code
rather than architecture text: the filesystem namespace, gated on all three targets and — on aarch64 —
over the real device.
Every P0 is closed, and the memory cluster is now complete end to end: address admission
ALET-P1-001, page-table reclamation ALET-P1-002, frame ownership ALET-P1-003, address-space
destruction ALET-P1-004, erase-on-free ALET-P2-026, per-arch attribute validation ALET-P1-008, W^X as
a global invariant ALET-P1-007, **x86-64 running on its own map ALET-P1-031**, and **the image
refusal unified in `kernel-core` ALET-P2-032**.

A frame can no longer be aliased, double-freed, leaked by unmapping, leaked by dying, or read by its
next owner. And on **all three targets** — not two — NO descriptor in the live address space is
writable+executable: kernel text is mapped read-only at 4 KiB granularity, and on x86-64 the
firmware's 524 795-violation tree is no longer translating at all rather than being reported beside
the one the kernel built. The last W^X hole is closed; what remains open in this cluster is layout
hardening (ALET-P1-006: guard pages, layout constants, KASLR posture), not permissions.
