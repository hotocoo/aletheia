# Aletheia — Requirement Traceability Matrix

**Machine-checkable** by `scripts/check-traceability.sh` (run in CI). This closes gap-register
**Issue 12** ("Establish Machine-Checkable Architecture Traceability"): every architectural
requirement has a stable identifier and maps to its ADR, implementation module, test, and VM gate,
so a claim of "delivered" is auditable against real evidence and **deferred work is explicitly
distinguished from implemented work**.

## How it is checked

The table below is the single source of truth. `scripts/check-traceability.sh` parses every row
whose Req ID begins with `REQ-` and enforces:

- **`delivered` / `partial`** — the Implementation and Test columns must name real files that exist
  in the tree (each `;`-separated path is checked), and any named VM Gate script must exist. A row
  marked delivered with a missing or `-` evidence path **fails the build** — the check that "CI can
  detect requirements marked delivered without evidence."
- **`deferred`** — evidence columns are `-` (nothing is built yet); the row documents the ADR /
  phased plan only. A deferred row is never counted as delivered.
- Every row's Status must be one of `delivered` / `partial` / `deferred`.
- **Target-specific rows** (GAPS2 Issue #1). Requirements whose implementation is per-CPU — kernel
  boot (`REQ-KERN-001/002/003`), memory (`REQ-MEM-{AARCH64,X86,RISCV}-001`), and user-mode
  (`REQ-USER-{AARCH64,X86,RISCV}-001`) — are split into one row **per target**, each naming that
  target's own implementation and its own VM gate (`scripts/vm-e2e.sh` for aarch64,
  `scripts/vm-e2e-x86.sh` for x86-64, `scripts/vm-e2e-riscv.sh` for RISC-V). This closes
  the hole where a generic row listed several impls but omitted a target's gate, so a target-specific
  regression could compile and escape the requirement gate. A regression in one target's user-mode now
  fails that target's named gate, not a sibling's.

Running a VM gate itself (booting QEMU) remains the existing `vm-e2e*` / `smoke-test` CI jobs; this
check verifies the **mapping** is real and that no delivered claim is evidence-free.

## Matrix

| Req ID | Title | ADR | Implementation | Test | VM Gate | Status |
|--------|-------|-----|----------------|------|---------|--------|
| REQ-CAP-001 | Fail-closed authorization (no capability ⇒ deny) | ADR-003 | kernel-core/src/spine.rs | kernel-core/tests/invariants.rs; kernel-core/tests/security_behavior.rs | scripts/vm-e2e.sh | delivered |
| REQ-CAP-002 | Unforgeable capability tokens | ADR-003 | kernel-core/src/spine.rs | kernel-core/tests/invariants.rs | scripts/vm-e2e.sh | delivered |
| REQ-CAP-003 | Delegation attenuation (never amplifies) | ADR-003 | kernel-core/src/spine.rs | kernel-core/tests/invariants.rs; kernel-core/tests/security_behavior.rs | scripts/vm-e2e.sh | delivered |
| REQ-CAP-004 | Cascading revocation | ADR-003 | kernel-core/src/spine.rs | kernel-core/tests/invariants.rs; kernel-core/tests/security_behavior.rs | scripts/vm-e2e.sh | delivered |
| REQ-CAP-005 | Policy/approval separation for destructive actions | ADR-015 | kernel-core/src/spine.rs; aletheia/src/policy.rs | kernel-core/tests/invariants.rs | - | delivered |
| REQ-CAP-006 | Capability concurrency semantics: atomic authorize+execute (no stale-authorization TOCTOU, no authority resurrection) — spec + hosted-proved primitive; SMP wiring deferred under REQ-SMP-001 | ADR-027 | kernel-core/src/spine.rs | kernel-core/tests/cap_concurrency.rs | - | delivered |
| REQ-CAP-007 | Authority lattice: attenuation defined once as a partial order (sound, reflexive, transitive), and distinct from the covering relation `evaluate` uses | ADR-048 | kernel-core/src/capalg.rs; kernel-core/src/spine.rs; aletheia/src/capabilities.rs | kernel-core/tests/capalg.rs; aletheia/tests/security.rs; docs/INVARIANT-CONTRACTS.md | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; scripts/vm-e2e-x86.sh | delivered |
| REQ-CAP-008 | Capability lifetime across a reboot with CUSTODY AS A LIFECYCLE (ADR-070): a persisted registry is untrusted input — attenuation, revocation cascade, clock monotonicity and id uniqueness re-verified on load, whole-image refusal; the sealed keystore/image pair derives versioned keys from a root handed in once, rotates through a one-way chain whose retirement destroys keys, constructs nonces without entropy and reserves them before use, rekeys through a three-commit pivot proved complete at every crash position; the root itself is DELIVERED over the platform firmware channel (ADR-072) with named refusals for absent/malformed/foreign and a monotone custody-generation witness in the entity store that catches rolled-back vaults | ADR-048; ADR-070; ADR-072 | kernel-core/src/capstore.rs; kernel-core/src/capvault.rs; kernel-core/src/bootroot.rs; kernel-core/src/fwcfg.rs; kernel-core/src/spine.rs | kernel-core/tests/capstore.rs; kernel-core/tests/capvault.rs; kernel-core/tests/bootroot.rs; docs/INVARIANT-CONTRACTS.md | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; scripts/vm-e2e-x86.sh; scripts/vm-e2e-vbox.sh | delivered |
| REQ-CON-003 | Keyboard input on the machine's own hardware: ACPI-declared i8042, enumerated and self-tested, decoded to the console's byte alphabet and fed into the SAME input ring the UART uses | ADR-049 | kernel-core/src/keymap.rs; kernel-x86_64/src/ps2.rs; kernel-x86_64/src/acpi.rs; kernel-x86_64/src/conirq.rs | kernel-core/tests/keymap.rs; docs/INVARIANT-CONTRACTS.md | scripts/keyboard-e2e.sh; scripts/vm-e2e-x86.sh; scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh | partial |
| REQ-PIPE-001 | Intent→Action pipeline: validate→authorize→execute→verify→record | ADR-002 | kernel-core/src/spine.rs | kernel-core/tests/invariants.rs | scripts/vm-e2e.sh | delivered |
| REQ-PIPE-002 | Malformed model output cannot execute | ADR-006 | kernel-core/src/spine.rs | kernel-core/tests/invariants.rs | scripts/vm-e2e.sh | delivered |
| REQ-STORE-001 | Content-addressed, versioned semantic store | ADR-005 | kernel-core/src/spine.rs; aletheia/src/storage.rs | kernel-core/tests/invariants.rs | - | delivered |
| REQ-STORE-002 | Encrypted at rest (ChaCha20-Poly1305) as a LIFECYCLE — versioned keys under a root, constructed per-frame nonces, position-bound frames, plaintext-identity semantics | ADR-005; ADR-069 | aletheia/src/storage.rs; aletheia/src/atrest.rs | aletheia/tests/acceptance.rs; aletheia/tests/encryption_at_rest.rs; docs/INVARIANT-CONTRACTS.md | - | delivered |
| REQ-IPC-001 | Capability-gated synchronous IPC | ADR-016 | kernel-core/src/ipc.rs | kernel-core/tests/invariants.rs | scripts/vm-e2e.sh | delivered |
| REQ-IPC-002 | Capability transfer with attenuation | ADR-016 | kernel-core/src/ipc.rs | kernel-core/tests/invariants.rs | - | delivered |
| REQ-IPC-003 | Bounded message queues | ADR-016 | kernel-core/src/ipc.rs | kernel-core/tests/invariants.rs | - | delivered |
| REQ-IPC-004 | Asynchronous notifications (coalescing badge) | ADR-020 | kernel-core/src/ipc.rs | kernel-core/tests/ipc.rs | - | delivered |
| REQ-IPC-005 | Deadline / timeout-aware receive | ADR-020 | kernel-core/src/ipc.rs | kernel-core/tests/ipc.rs | - | delivered |
| REQ-IPC-006 | Message cancellation (contract: INV-IPC-CANCEL) | ADR-020 | kernel-core/src/ipc.rs | kernel-core/tests/ipc.rs; docs/INVARIANT-CONTRACTS.md | - | delivered |
| REQ-IPC-007 | IPC tracing + deterministic replay | ADR-020 | kernel-core/src/ipc.rs | kernel-core/tests/ipc.rs | - | delivered |
| REQ-IPC-008 | Zero-copy shared-memory channels (policy + all-target real MMU path) | ADR-020 | kernel-core/src/grant.rs; kernel/src/usermode.rs; kernel-x86_64/src/usermode.rs; kernel-riscv64/src/usermode.rs | kernel-core/tests/grant.rs | scripts/vm-e2e.sh; scripts/vm-e2e-x86.sh; scripts/vm-e2e-riscv.sh | delivered |
| REQ-IPC-009 | Priority inheritance / donation | ADR-020 | kernel-core/src/priosched.rs; kernel/src/usermode.rs; kernel-riscv64/src/usermode.rs; kernel-x86_64/src/usermode.rs | kernel-core/tests/priosched.rs; kernel/src/usermode.rs; kernel-riscv64/src/usermode.rs; kernel-x86_64/src/usermode.rs | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; scripts/vm-e2e-x86.sh | delivered |
| REQ-IPC-010 | Blocking IPC (recv blocks, send wakes + delivers, receiver resumes) | ADR-020 | kernel/src/usermode.rs; kernel-riscv64/src/usermode.rs; kernel-x86_64/src/usermode.rs | kernel/src/usermode.rs; kernel-riscv64/src/usermode.rs; kernel-x86_64/src/usermode.rs | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; scripts/vm-e2e-x86.sh | delivered |
| REQ-COMP-001 | WASM components, no ambient authority, with an EXPLICITLY VERSIONED ABI (ADR-066): guests declare their ABI in a custom section, both install and run refuse undeclared/malformed/foreign versions by name, installed metadata stamps the admitted version, and the v1 host import surface is pinned by a live link probe | ADR-014; ADR-066 | aletheia/src/component.rs; aletheia/src/syscore.rs; component-sdk/src/lib.rs | aletheia/tests/component.rs; aletheia/tests/component_abi.rs | - | delivered |
| REQ-COMP-002 | Fuel-bounded component execution, and a resource model BEYOND fuel (ADR-065): memory bytes, table elements, stack height/recursion depth and a wall-clock deadline enforced at host-call crossings — fail-closed defaults, named kills in the outcome and the audit event, spawn tree inherits the root envelope | ADR-014; ADR-065 | aletheia/src/component.rs; aletheia/src/syscore.rs | aletheia/tests/component.rs; aletheia/tests/component_resources.rs | - | delivered |
| REQ-COMP-003 | Multi-agent spawn with attenuated delegation, where RESOLVING A DEPENDENCY IS ITSELF AUTHORIZED (ADR-068): component.spawn evaluated at the guest boundary and re-evaluated by the host at fulfilment, scoped grants pin the resolvable dependency set, approval-constrained and revoked grants refuse inline | ADR-014; ADR-068 | aletheia/src/component.rs; aletheia/src/syscore.rs | aletheia/tests/component.rs; aletheia/tests/component_dependencies.rs | - | delivered |
| REQ-COMP-004 | Rust component SDK | ADR-014 | component-sdk/src/lib.rs | aletheia/tests/sdk_component.rs | - | delivered |
| REQ-COMP-005 | Component property / chaos gate | ADR-014 | aletheia/src/component.rs | aletheia/tests/component_chaos.rs | - | delivered |
| REQ-EXP-001 | Capability-gated World-Model search | ADR-018 | aletheia/src/ai/context.rs | aletheia/tests/search.rs | - | partial |
| REQ-EXP-002 | Native desktop / compositor / dynamic UI | ADR-009 | - | - | - | deferred |
| REQ-SVC-001 | Service API / IPC boundary + daemon | ADR-016 | aletheia/src/service.rs | aletheia/tests/conformance.rs | - | delivered |
| REQ-AI-001 | Model-agnostic, untrusted AI provider | ADR-017 | aletheia/src/ai/mod.rs; aletheia/src/ai/runtime.rs | aletheia/tests/acceptance.rs | - | delivered |
| REQ-AI-002 | Context Fabric (capability-aware, not RAG) | ADR-018 | aletheia/src/ai/context.rs | aletheia/tests/search.rs | - | delivered |
| REQ-AI-003 | AI execution substrate + heterogeneous CPU/GPU/NPU scheduler | ADR-022 | - | - | - | deferred |
| REQ-AI-004 | Pinned model registry + operator-selectable resident model | ADR-052 | aletheia/src/ai/registry.rs; aletheia/src/ai/mod.rs; models/lfm2.5.toml; models/minicpm.toml; models/aletheia-lm.toml | aletheia/tests/model_registry.rs | - | delivered |
| REQ-AI-005 | Operation-surface benchmark with served-model identity check | ADR-052 | aletheia/src/ai/bench.rs; aletheia/src/ai/llama.rs | aletheia/tests/model_registry.rs | - | delivered |
| REQ-AI-006 | Kernel-console command surface as a planning target (native tool calling + context brief), gated live; destructive lines go through the Core's pending-approval surface, bound to the exact rendered line and spent once (ADR-059) | ADR-053; ADR-059 | aletheia/src/console_ops.rs; aletheia/src/ai/console.rs; aletheia/src/ai/llama.rs; kernel-core/src/shell.rs; aletheia/src/policy.rs; aletheia/src/syscore.rs | aletheia/tests/console_plan.rs; aletheia/tests/console_approvals.rs | scripts/console-ai-e2e.sh | delivered |
| REQ-AI-007 | Multi-step console session: one request becomes a bounded sequence in which the model reads the machine's answer to its own previous command, with a step budget, no-progress detection and a refusal to end the session it is observing | ADR-054 | aletheia/src/ai/agent.rs; aletheia/src/ai/llama.rs; aletheia/src/console_ops.rs | aletheia/src/ai/agent.rs | scripts/console-agent-e2e.sh | delivered |
| REQ-AI-008 | Correctable proposals: a step Aletheia can name the fault in is handed back to the model and re-asked (bounded, and never charged to the console budget), while missing authority and the loop's own bounds stay terminal | ADR-055 | aletheia/src/ai/agent.rs; aletheia/src/console_ops.rs | aletheia/src/ai/agent.rs | scripts/console-agent-e2e.sh | delivered |
| REQ-AI-009 | Staleness-aware no-progress (a reading repeats only within the window since the machine last changed; a mutation never repeats), and a gate that asserts any sufficient command rather than one preferred spelling | ADR-055 | aletheia/src/ai/agent.rs | aletheia/src/ai/agent.rs | scripts/console-agent-e2e.sh | delivered |
| REQ-AI-010 | Bounded, measured agent turns: a whole-transcript byte bound that elides the oldest output visibly, and per-turn wall time and model-call counts reported so speed is measured rather than claimed | ADR-055 | aletheia/src/ai/agent.rs; aletheia/src/main.rs | aletheia/src/ai/agent.rs | scripts/console-agent-e2e.sh | delivered |
| REQ-KERN-001 | aarch64 microkernel boots + spine invariants | ADR-019 | kernel/src/main.rs | kernel-core/tests/invariants.rs | scripts/vm-e2e.sh | delivered |
| REQ-KERN-002 | x86-64 bootable image + spine invariants | ADR-019 | kernel-x86_64/src/main.rs | kernel-core/tests/invariants.rs | scripts/vm-e2e-x86.sh | delivered |
| REQ-KERN-003 | RISC-V microkernel + spine invariants | ADR-019 | kernel-riscv64/src/main.rs | kernel-core/tests/invariants.rs | scripts/vm-e2e-riscv.sh | delivered |
| REQ-KERN-004 | Shared kernel-core spine + selftest (Issue 1 slice) | ADR-019 | kernel-core/src/spine.rs; kernel-core/src/selftest.rs | kernel-core/tests/invariants.rs | scripts/vm-e2e.sh | delivered |
| REQ-KERN-005 | Shared kernel-core task/scheduler policy (Issue 1 rest) | ADR-019 | kernel-core/src/sched.rs; kernel/src/usermode.rs | kernel-core/tests/sched.rs | scripts/vm-e2e.sh | delivered |
| REQ-CONF-001 | Cross-architecture semantic conformance gate (GAPS2 #2) | ADR-019 | scripts/conformance.sh | scripts/conformance.sh | scripts/conformance.sh | delivered |
| REQ-QUAL-001 | CI-coverage gate (GAPS4 ALET-P0-003): every bootable architecture discovered in the tree must have a boot gate CI executes, every gate script in scripts/ must be executed by CI or explicitly exempt with a stated reason, every VM gate this matrix claims must actually be executed by CI, and STATUS.md must document every script CI runs. Traceability proves evidence EXISTS; this proves it RUNS | ADR-012 | scripts/check-ci-parity.sh | scripts/check-ci-parity.sh | scripts/check-ci-parity.sh | delivered |
| REQ-MM-001 | Mapping-API admission check: raw `va`/`pa` are untrusted input. One arch-independent rule set (decoded-VA-width aliasing, ISA canonical form, page alignment, null page, frame-allocator window) enforced fail-closed at the entry of every mapping API on all three targets. Host-proved as properties (no two accepted VAs may alias; every accepted PA is an owned frame) and VM-gated on live page tables per target, with the refusals part of the cross-architecture conformance contract | ADR-029 | kernel-core/src/vmaddr.rs; kernel/src/vm.rs; kernel-riscv64/src/vm.rs; kernel-x86_64/src/vm.rs | kernel-core/tests/vmaddr.rs; kernel/src/vm.rs; kernel-riscv64/src/vm.rs; kernel-x86_64/src/vm.rs | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; scripts/vm-e2e-x86.sh; scripts/conformance.sh | delivered |
| REQ-MM-002 | Physical frame ownership: every frame has exactly one owner (kernel / page-table / user / per-address-space tag), claimed before it leaves the free list and released only by its holder. Double free, free of a never-allocated frame, free of another owner's frame, and claim of a live frame are named, fail-closed refusals; a refusal is a total no-op. Host-proved as properties over a deterministic 20 000-operation sequence, wired into all three targets' allocators, VM-gated (17 memory invariants per target) and part of the cross-architecture conformance contract | ADR-030 | kernel-core/src/frameown.rs; kernel/src/frames.rs; kernel-riscv64/src/frames.rs; kernel-x86_64/src/frames.rs | kernel-core/tests/frameown.rs; kernel/src/frames.rs; kernel-riscv64/src/frames.rs; kernel-x86_64/src/frames.rs | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; scripts/vm-e2e-x86.sh; scripts/conformance.sh | delivered |
| REQ-MM-003 | Page-table reclamation: an unmap that empties an intermediate table frees it — empty-only, parent reference cleared before the free, root never freed, stop at the first table still in use, and a refused (ownership-checked) free restores the reference. Shared policy in kernel-core with a per-target `TableOps` seam, host-proved against an in-memory page-table model, VM-gated on live hierarchies (33 vm invariants on aarch64/RISC-V, 25 on x86-64) and part of the cross-architecture conformance contract by behavior (level counts differ honestly: 3-level walks reclaim two tables, 4-level three) | ADR-031 | kernel-core/src/ptreclaim.rs; kernel/src/vm.rs; kernel-riscv64/src/vm.rs; kernel-x86_64/src/vm.rs | kernel-core/src/ptreclaim.rs; kernel/src/vm.rs; kernel-riscv64/src/vm.rs; kernel-x86_64/src/vm.rs | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; scripts/vm-e2e-x86.sh; scripts/conformance.sh | delivered |
| REQ-MM-004 | Address-space destruction: a dying space returns every page and table it owns, and nothing else — depth-first, entries cleared before each free, root last, guarded independently by a per-target privacy predicate (shared kernel slots are never walked) and by the ownership model (unowned block/device/granted leaves are skipped and counted). Destroying the ACTIVE address space is refused on every target. Host-proved against an in-memory model, VM-gated on live hierarchies (aarch64/RISC-V 42 vm invariants, x86-64 33) with the frame count returning exactly to its pre-space value, and part of the cross-architecture conformance contract | ADR-032 | kernel-core/src/teardown.rs; kernel/src/vm.rs; kernel-riscv64/src/vm.rs; kernel-x86_64/src/vm.rs | kernel-core/src/teardown.rs; kernel/src/vm.rs; kernel-riscv64/src/vm.rs; kernel-x86_64/src/vm.rs | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; scripts/vm-e2e-x86.sh; scripts/conformance.sh | delivered |
| REQ-MM-005 | Erase on free: a released frame is zeroed at the single choke point every return path goes through (explicit free, page-table reclamation, address-space teardown), after the ownership check and before the free-list link is written, so no frame ever carries a previous OWNER's bytes. Proved on each target by the reuse case — write a pattern, free, re-allocate the same frame (LIFO), require zeros — not by asserting `alloc_zeroed`; part of the cross-architecture conformance contract | ADR-033 | kernel/src/frames.rs; kernel-riscv64/src/frames.rs; kernel-x86_64/src/frames.rs | kernel/src/frames.rs; kernel-riscv64/src/frames.rs; kernel-x86_64/src/frames.rs | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; scripts/vm-e2e-x86.sh; scripts/conformance.sh | delivered |
| REQ-MM-006 | W^X and memory-attribute validation: one arch-neutral permission model (no write+execute, no executable device memory, no user page executable at kernel privilege, no user-exec without user-access) enforced at the entry of every dynamic mapping API on all three targets, plus an audit that walks a live hierarchy and counts violations by class. User code pages are RX, kernel dynamic pages NX, and x86-64 enables EFER.NXE + CR4.SMEP (reported at boot, not assumed). On both QEMU targets W^X is a GLOBAL invariant of the map the kernel builds: `linker.ld` exports the section boundaries and `build_identity` builds every RAM block overlapping the image as 4 KiB pages (text RO+X, `.rodata` RO+NX, data/bss/stack/heap RW+NX; RAM outside the image RW+NX blocks), so both gates require `dynamic_violations == 0` AND `bootstrap_violations == 0` plus four invariants that assert the split's granularity and per-section permissions and two that assert `map_page`/`unmap_page` refuse the split span (the block descriptors that used to make kernel-image VAs unmappable-over are gone, so the refusal is explicit) — ALET-P1-007 resolved. **x86-64 no longer adopts the OVMF tree (ALET-P1-031 resolved):** `kernel-x86_64/src/kmap.rs` takes the image base/size from `LoadedImage` before ExitBootServices and the per-section permissions from the image's own PE section table (the PE equivalent of the `linker.ld` symbols), builds an identity map — 2 MiB RW+NX huge pages for RAM and sub-4 GiB MMIO, 4 KiB pages over every region the image touches — proves nine invariants over it, and only then writes CR3 (cycling CR4.PGE, since OVMF marks its mappings global and a global TLB entry survives a CR3 load). Everything after that point — spine, 4-core SMP, the whole ring-3 suite — runs on the kernel's own tree; the live audit is 4603 leaves / **0 violations** against the firmware tree's 524 795, the boot fails closed (exit 28) on any live violation, and the gate requires the ACTIVE marker plus zero live violations rather than pinning a count. The image-remap refusal itself lives ONCE, as `AddrPlan::with_protected` + `MapFault::ProtectedVirt` in `kernel-core/src/vmaddr.rs`, with each target declaring only where its image is (ALET-P2-032 resolved). Not claimed: devices above 4 GiB are unmapped by the x86-64 tree | ADR-034 | kernel-core/src/memattr.rs; kernel-core/src/vmaddr.rs; kernel/src/vm.rs; kernel-riscv64/src/vm.rs; kernel-x86_64/src/vm.rs; kernel-x86_64/src/kmap.rs | kernel-core/src/memattr.rs; kernel-core/tests/vmaddr.rs; kernel/src/vm.rs; kernel-riscv64/src/vm.rs; kernel-x86_64/src/vm.rs | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; scripts/vm-e2e-x86.sh; scripts/conformance.sh | delivered |
| REQ-MEM-AARCH64-001 | Physical frame allocator + MMU virtual memory (aarch64) | ADR-019 | kernel/src/frames.rs; kernel/src/vm.rs | kernel/src/vm.rs | scripts/vm-e2e.sh | delivered |
| REQ-MEM-X86-001 | Physical frame allocator (UEFI map) + MMU map/unmap over live PML4 (x86-64) | ADR-019 | kernel-x86_64/src/frames.rs; kernel-x86_64/src/vm.rs | kernel-x86_64/src/vm.rs | scripts/vm-e2e-x86.sh | delivered |
| REQ-MEM-RISCV-001 | Physical frame allocator + Sv39 MMU virtual memory (RISC-V) | ADR-019 | kernel-riscv64/src/frames.rs; kernel-riscv64/src/vm.rs | kernel-riscv64/src/vm.rs | scripts/vm-e2e-riscv.sh | delivered |
| REQ-USER-AARCH64-001 | EL0 user-mode: cap-gated syscall + per-process address space + preemptive multitasking (aarch64) | ADR-019 | kernel/src/usermode.rs | kernel/src/usermode.rs | scripts/vm-e2e.sh | delivered |
| REQ-USER-X86-001 | ring-3 user-mode: cap-gated syscall + per-process PML4 + PIT preemption (x86-64) | ADR-019 | kernel-x86_64/src/usermode.rs | kernel-x86_64/src/usermode.rs | scripts/vm-e2e-x86.sh | delivered |
| REQ-USER-RISCV-001 | U-mode: cap-gated ecall + per-process satp + S-timer preemption (RISC-V) | ADR-019 | kernel-riscv64/src/usermode.rs | kernel-riscv64/src/usermode.rs | scripts/vm-e2e-riscv.sh | delivered |
| REQ-SMP-001 | SMP / multicore umbrella — DELIVERED across every slice: secondary bring-up (REQ-SMP-002), per-CPU run queues + work stealing (REQ-SMP-003), cross-core TLB shootdown (REQ-SMP-004), and CPU affinity + cross-core task migration + the lock-hierarchy/atomic-ordering audit (REQ-SMP-005, ADR-028). NUMA-aware placement remains a documented later refinement (ADR-021), not an open gap | ADR-021 | kernel-core/src/smpsched.rs; kernel/src/smp.rs; kernel-riscv64/src/smp.rs; kernel-x86_64/src/smp.rs | kernel-core/tests/smpsched.rs; kernel/src/smp.rs | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; scripts/vm-e2e-x86.sh | delivered |
| REQ-SMP-002 | Secondary-CPU bring-up + cross-core concurrency substrate (PSCI CPU_ON / SBI HSM hart_start / MADT + INIT-SIPI-SIPI real-mode trampoline, per-CPU TPIDR/tp/GS_BASE, shared SpinLock, atomic authorize+execute on real cores, SGI/SSIP/LAPIC IPI) — ALL THREE targets, `-smp 4` VM-gated | ADR-021 | kernel/src/smp.rs; kernel/src/boot.s; kernel-riscv64/src/smp.rs; kernel-riscv64/src/boot.s; kernel-x86_64/src/smp.rs; kernel-core/src/sync.rs | kernel/src/smp.rs; kernel-riscv64/src/smp.rs; kernel-x86_64/src/smp.rs; kernel-core/tests/sync.rs | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; scripts/vm-e2e-x86.sh | delivered |
| REQ-SMP-003 | Per-CPU run queues + cross-core work stealing (ADR-021 Phase 2 policy): exactly-once dispatch, local-first, steal-from-most-loaded, never-two-locks discipline — kernel-core policy host-proved under real threads + VM-gated on real cores on ALL THREE targets | ADR-021 | kernel-core/src/smpsched.rs; kernel/src/smp.rs; kernel-riscv64/src/smp.rs; kernel-x86_64/src/smp.rs | kernel-core/tests/smpsched.rs; kernel/src/smp.rs; kernel-riscv64/src/smp.rs; kernel-x86_64/src/smp.rs | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; scripts/vm-e2e-x86.sh | delivered |
| REQ-SMP-004 | Cross-core TLB shootdown: arch-independent all-acknowledged barrier (post invalidation to a target set, block until every target ran its local invalidation AND acknowledged, fail-closed on an unresponsive/offline CPU) + per-target real-core wiring — aarch64 broadcast `tlbi …is`, x86-64 `invlpg` (no HW broadcast), RISC-V SBI RFENCE; each target Phase 6 primes frame A, remaps to frame B, shoots down, observes B coherently, `-smp 4` VM-gated. Discriminating stale-vs-fresh proof is the host-thread barrier test (QEMU TCG does not faithfully model stale-TLB retention) | ADR-021 | kernel-core/src/shootdown.rs; kernel/src/smp.rs; kernel-riscv64/src/smp.rs; kernel-x86_64/src/smp.rs | kernel-core/tests/shootdown.rs; kernel/src/smp.rs; kernel-riscv64/src/smp.rs; kernel-x86_64/src/smp.rs | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; scripts/vm-e2e-x86.sh | delivered |
| REQ-SMP-005 | CPU affinity + preemptive cross-core task migration + lock-hierarchy/atomic-ordering audit (the REQ-SMP-001 tail). Affinity mask lives INLINE in each run-queue element (bounded-rotate affinity-aware steal, never a second locked side-table); migration = a task enqueued on one CPU dispatched AND resumed on another through the `sched::TaskContext` seam; the "at most one queue lock per CPU" discipline is enforced by a per-instance debug tripwire, with a per-atomic-site ordering audit (ADR-028). kernel-core policy host-proved (mixed-affinity exactly-once under 4-thread contention, affinity-honored, migration-through-seam, tripwire fires) + VM-gated on ALL THREE targets (SMP invariants 19-21, 22 total each, `-smp 4`) | ADR-028 | kernel-core/src/smpsched.rs; kernel/src/smp.rs; kernel-riscv64/src/smp.rs; kernel-x86_64/src/smp.rs | kernel-core/tests/smpsched.rs; kernel/src/smp.rs; kernel-riscv64/src/smp.rs; kernel-x86_64/src/smp.rs | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; scripts/vm-e2e-x86.sh | delivered |
| REQ-ML-001 | In-kernel risk advice from a frozen integer forest: a gradient-boosted forest trained OUTSIDE this repo (`aletheia-ml`, Google Borg trace) compiled to a flat `ALTM1` table of `i32` compares and evaluated in `no_std` with NO floating point and no allocation after load. ADVISORY ONLY (ADR-056): it may reorder tasks of EQUAL effective priority and nothing else; every invariant and capability check holds identically when the model is loaded, absent, or wrong; an abstaining or absent model schedules bit-identically to the model-free kernel; and every malformed blob (magic, version, feature count, feature-contract hash, fixed-point scale, truncation, backwards child index) is a NAMED refusal, never a silent degradation. Parity with the trainer is a committed fixture asserted exactly in Rust, not a claim. The blob is embedded in every kernel image (`include_bytes!`) and VERIFIED AT BOOT on all three targets, where a 20-invariant suite re-proves — in kernel space, against the image's own bytes — load verification, the measured worst-case compare bound, exact margin/verdict parity over the whole fixture, determinism, out-of-box abstention, all nine named refusals, and the two advisory properties (an abstaining model schedules bit-identically to the model-free kernel; priority is never traded for risk). Deriving the feature vector from live kernel tasks is deferred, named (ADR-056): the suite is on the boot path, real-task feature extraction is not | ADR-056 | kernel-core/src/mlrisk.rs; kernel-core/src/mlrisk_contract.rs; kernel-core/src/priosched.rs; kernel-core/models/aletheia_risk.altm | kernel-core/tests/mlrisk.rs; kernel-core/tests/priosched.rs | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; scripts/vm-e2e-x86.sh | delivered |
| REQ-ML-002 | Risk advice measured UNDER LOAD on the real target, and the advisory properties gated at a scale a scheduler actually runs at. `worst_case_compares` is arithmetic; this is a measurement: each target times `RiskAdvisor::advise` with its own `Hal` clock over thousands of calls and prints ps/advice and advices/s, plus a verdict census that separates the TWO abstain causes (conformal band vs training-box range guard), plus an A/B of the SAME admission stream scheduled twice — model-free and advised — reporting how many positions the advice actually moves. Timings are REPORTED, never gated (QEMU-TCG nanoseconds are an emulator's). Eight scale-invariant checks ARE gated on all three targets: the census accounts for every advice, the range guard stays silent on in-box rows and fires on out-of-box arrivals, the same load reproduces the same margins and census exactly, the advised schedule is a PERMUTATION of the model-free one (no task invented, dropped or starved) under both an all-tied and a priority-banded workload and over the trainer's own held-out rows, and a workload the model abstains on drains in EXACTLY the model-free order. Running this gate is what exposed a real kernel defect: `PriorityScheduler::effective_priority` allocated a `BTreeSet` per priority comparison, so an O(n^2) drain consumed 7.7 MB of an 8 MiB never-freeing bump heap and OOM'd the rest of boot; the donation fast path fixed it to 0.87 MB and the gate now passes on all three targets | ADR-056 | kernel-core/src/mlrisk_stress.rs; kernel-core/src/mlrisk.rs; kernel-core/src/priosched.rs | kernel-core/tests/mlrisk_stress.rs; kernel-core/tests/priosched.rs | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; scripts/vm-e2e-x86.sh | delivered |
| REQ-ML-003 | The risk forest is RESIDENT and CONSULTED while the machine runs, and it is consulted about REAL tasks. Closes the gap `docs/MATURITY.md` had named since the advisor landed: nothing derived the 20-feature vector from a live task, so on a running machine the model advised about no one. `taskfeat.rs` derives that vector from live kernel state under the TRAINER'S OWN accounting rules — exclusive history (a task is never counted in its own features), cell pressure read from the COMPLETED five-minute bin, the contract's fixed point, an unobservable field reported as `missing_info` rather than as a zero measurement, and a BOUNDED history so a month-long uptime cannot grow an unbounded map on a never-freeing kernel heap. `mlsched.rs` holds one verified forest behind one lock for the machine's whole uptime, owns the live history, and is the path a task takes to `PriorityScheduler`; every dispatch and every task death feeds back into what the NEXT advice reads. Twelve live-advisory invariants are re-proved on every boot of every target, including the advisory one — with no model resident the live path schedules BIT-IDENTICALLY to the model-free kernel — and priority is never traded for risk. Each boot commissions the advisor against 4096 real admissions spanning 95 cell bins and proves the advised drain is a PERMUTATION of the model-free one. `mlstat` at the console reports the live counters at any later moment, including `silence`: seconds since the last consultation as of the machine's own clock, which is what makes 'continuously active' falsifiable (a historical max-gap cannot fall, so it alone would not). Running this landed a real finding: the shipped borg2019 blob's `disk_request` training range is literally [0,0] — that corpus carries no per-task disk signal — so a kernel supplying a real disk fraction would place EVERY task outside the training box and the advisor would correctly abstain about the entire machine. A REAL user-mode task reaches it: aarch64 and RISC-V each run two genuine ring-3/U-mode tasks — own address spaces, own trap frames, real context switches — admitted through `resident::admit` and dispatched by `PriorityScheduler`, with dispatch and exit fed back, described with the memory they actually mapped (`run_advised_scheduler`, three gated boot invariants per target, EL0/U-mode suites now 32). The interleaving is deliberately NOT asserted — advice may reorder equals, and demanding a fixed order would assert the advisor had no effect; that every task gets every slice and exits IS asserted. ALL THREE TARGETS are wired, x86-64 last and deliberately so: its ring-3 gate was red on a defect predating this work, and wiring a model into a target whose user-mode gate cannot pass proves nothing about either. That defect is fixed in the same wave — `SYS_REGCHECK` returned 0, which landed in the task's `rax`, whose sentinel IS `SYS_REGCHECK`, so the stub's second `int 0x80` dispatched syscall 0 and the RESTORE half of ALET-P1-009 had never once been exercised on x86-64. The handler now echoes the call number. Ring-3/EL0/U-mode suites: 39/32/32, all three gates PASS, `e2e-all.sh` PASS, `console-e2e.sh` PASS on all three | ADR-056 | kernel-core/src/taskfeat.rs; kernel-core/src/mlsched.rs; kernel-core/src/shell.rs; kernel-core/src/priosched.rs | kernel-core/tests/mlsched.rs; kernel-core/tests/priosched.rs | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh | delivered |
| REQ-ML-004 | The advice is shown to HELP, and shown against what every major OS family actually does. A classifier metric cannot answer this: a scheduler does not run a classifier, it runs a queue. `aletheia-ml`'s `schedsim` replays the untouched chronological test split through the kernel's OWN selection rule twice, changing exactly one thing — among EQUAL priorities, prefer a decisive `low` over a decisive `elevated` — with verdicts from the exported INTEGER forest and labels from the trace's own terminal events. Head-of-line delay for surviving tasks fell in 3 of 3 independent 60000-task windows, mean 3.99% (min 1.43%, max 7.19%), dispatch counts identical in every arm. `oscompare` then implements the DOCUMENTED selection rule of fifteen arms — Linux CFS, EEVDF, BORE (CachyOS/Zen), MuQSS/BFS, PREEMPT_RT SCHED_FIFO, Windows NT, Darwin/XNU, FreeBSD ULE, Solaris TS, Fuchsia Zircon, seL4, Redox, FIFO and both Aletheia arms — and drives all of them from one trace with identical arrivals, queue depth and service demands. It also publishes the decomposition that keeps it honest: of the advised arm's whole lead, only 1.50 points come from the MODEL; the rest is strict priority scheduling, which any kernel could adopt, and the gain is bought with longer waits for doomed tasks. NOT CLAIMED: nothing here boots Linux, Windows, macOS, FreeBSD, Solaris, Fuchsia, seL4 or Redox, and these numbers say nothing about which operating system is faster — they compare pick functions under one workload, with no preemption latency, cache, NUMA, wakeup placement, load balancing or energy model | ADR-056 | docs/evidence/ml004/schedsim.py; docs/evidence/ml004/oscompare.py | docs/evidence/ml004/schedsim_kernel.json; docs/evidence/ml004/oscompare_kernel.json | - | delivered |
| REQ-ML-005 | A second frozen forest for MEMORY reclaim, trained on the eviction event specifically. `mlrisk` answers a scheduling question and carries a scheduler's cost function; reclaim asks whose pages should go when frames are short, and the mistake that hurts is different — taking frames from a task that would have completed destroys the work it had already done. `aletheia-ml`'s `memrisk` rebuilds the label from the trace's own terminal events (eviction only, distinct from fail/kill/lost), keeps the SAME 20-feature contract the kernel already derives so no second contract hash can drift, chooses the operating threshold against a NAMED asymmetric cost (false reclaim 4.0 vs missed reclaim 1.0) rather than an unchosen 0.5, and exports an `ALTM1` blob the kernel's existing `RiskAdvisor::load` verifies with no new code. Measured on an untouched chronological test split of 696877 rows at a 0.2561 eviction base rate: PR-AUC 0.8818 (lift 3.44x), ROC-AUC 0.9392, precision 0.9867, recall 0.5957, expected cost 0.1117/task against 0.2561 (reclaim nothing) and 2.9757 (reclaim everything), integer-vs-float decision agreement 1.000. NOT CLAIMED AND NOT WIRED: `ptreclaim.rs` and `frameown.rs` do not consult it, the features are derived at task SUBMISSION rather than under pressure, and no boot-gated stress or A/B invariants exist for it — the scheduler forest needed a separate wave to go from exported to resident, and this one has not had it | ADR-056 | ../aletheia-ml/src/aletheia_ml/memrisk.py; ../aletheia-ml/notebooks/aletheia_ram_management.ipynb | ../aletheia-ml/artifacts/borg2019/memrisk_metrics.json | - | deferred |
| REQ-SEC-001 | Adversarial security-behaviour regressions | ADR-003 | kernel-core/src/spine.rs | kernel-core/tests/security_behavior.rs | - | delivered |
| REQ-DRV-001 | Device / driver architecture (discovery, real driver, hotplug, DMA/IOMMU, restart) | ADR-023 | kernel-core/src/device.rs | kernel-core/tests/device.rs | - | partial |
| REQ-DRV-002 | Capability-authorized device access (hosted, over the BlockDevice seam) | ADR-023 | kernel-core/src/device.rs | kernel-core/tests/device.rs | - | delivered |
| REQ-DRV-003 | virtio-blk driver over virtio-mmio (modern/v2): discovery + split-virtqueue + BlockDevice, VM-gated | ADR-023 | kernel/src/virtio.rs | kernel/src/virtio.rs | scripts/vm-e2e.sh | delivered |
| REQ-DRV-004 | virtio-blk defined ONCE in kernel-core behind a two-function per-target seam; RISC-V (first-class) gains real storage | ADR-036 | kernel-core/src/virtioblk.rs; kernel/src/virtio.rs; kernel-riscv64/src/virtio.rs | kernel-core/tests/virtioblk.rs | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh | delivered |
| REQ-DRV-005 | virtio-pci transport (PCI enumeration + capability-described BARs) + device-memory admission rule; x86-64 gains real storage | ADR-037 | kernel-x86_64/src/pci.rs; kernel-x86_64/src/virtio.rs; kernel-core/src/vmaddr.rs | kernel-core/tests/vmaddr.rs; kernel-core/tests/virtioblk.rs | scripts/vm-e2e-x86.sh | delivered |
| REQ-STOR-004 | Storage error semantics contract (kinds distinguishable, device errors surfaced incl. flush, cause preserved, refusals are no-ops) | ADR-024 | kernel-core/src/storage.rs; kernel-core/src/fs.rs | kernel-core/tests/storage.rs; docs/INVARIANT-CONTRACTS.md | - | delivered |
| REQ-STOR-001 | Persistent storage stack (driver → FS/object store → encryption → semantic store) | ADR-024 | kernel-core/src/storage.rs; kernel/src/virtio.rs | kernel-core/tests/storage.rs; kernel/src/virtio.rs | scripts/vm-e2e.sh | partial |
| REQ-STOR-002 | Crash-consistent journaled block store (WAL over a BlockDevice seam, hosted) | ADR-024 | kernel-core/src/storage.rs | kernel-core/tests/storage.rs | - | delivered |
| REQ-FS-001 | Filesystem namespace over the journal (named objects; create/remove is ONE transaction; erase on delete) | ADR-035 | kernel-core/src/fs.rs | kernel-core/tests/fs.rs | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; scripts/vm-e2e-x86.sh | delivered |
| REQ-STOR-003 | Durable spine store: whole-store atomic save (fs replace), content-address + record-checksum verified load, cross-reboot witness | ADR-038 | kernel-core/src/persist.rs; kernel-core/src/fs.rs; kernel-core/src/spine.rs | kernel-core/tests/persist.rs; kernel-core/tests/fs.rs | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; scripts/vm-e2e-x86.sh | delivered |
| REQ-QUAL-003 | Gap-register self-check: resolved rows' evidence exists, cited requirements are tracked, rollup arithmetic matches the rows | ADR-013 | scripts/check-register.sh | scripts/check-register.sh | - | delivered |
| REQ-DRV-006 | DMA boundary: a device is only told about memory a driver registered (deny by default, never the kernel image, revocable) | ADR-043; ADR-073 (hardware rung) | kernel-core/src/dma.rs; kernel-core/src/vtd.rs; kernel-x86_64/src/vtd.rs | kernel-core/src/dma.rs; kernel-core/src/vtd.rs; kernel-core/tests/vtd.rs; scripts/conformance.sh | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; scripts/vm-e2e-x86.sh | delivered |
| REQ-NET-001 | virtio-net driver + reusable multi-queue virtqueue substrate (rx buffers posted before DRIVER_OK) | ADR-041 | kernel-core/src/virtq.rs; kernel-core/src/virtionet.rs; kernel/src/virtio.rs; kernel-riscv64/src/virtio.rs; kernel-x86_64/src/virtio.rs | kernel-core/src/virtionet.rs; scripts/conformance.sh | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; scripts/vm-e2e-x86.sh | delivered |
| REQ-NET-002 | Minimal network stack: ARP resolution + ICMP echo with verified checksums, against a real peer | ADR-041 | kernel-core/src/virtionet.rs | kernel-core/src/virtionet.rs; scripts/conformance.sh | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; scripts/vm-e2e-x86.sh | delivered |
| REQ-NET-003 | UDP datagram service + ARP cache + DHCP discovery (second slice, ALET-P2-020): bounded byte-exact LRU cache whose silence on the wire is counted; IPv4/UDP build+parse fail-closed at both ends over ONE internet checksum and a pseudo-header that catches re-addressing in flight (zero checksums refused by name); DISCOVER→OFFER with the transaction id bound and the offered address cross-checked against the driver's claim | ADR-060 | kernel-core/src/arpcache.rs; kernel-core/src/udpv4.rs; kernel-core/src/dhcp.rs; kernel-core/src/virtionet.rs | kernel-core/tests/netstack.rs; scripts/conformance.sh | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; kernel-x86_64/scripts/smoke-test.sh | delivered |
| REQ-GFX-001 | virtio-gpu driver, first slice (ALET-P2-021): descriptor chains in the substrate, display-info round trip, the whole 2D resource lifecycle (create/attach/set-scanout/transfer/flush/detach/unref), local refusals proven silent by a traffic counter, and the lifecycle proven by the DEVICE's own error grammar — on all three targets, two buses | ADR-057 | kernel-core/src/virtq.rs; kernel-core/src/virtiogpu.rs; kernel/src/virtio.rs; kernel-riscv64/src/virtio.rs; kernel-x86_64/src/virtio.rs | kernel-core/tests/virtiogpu.rs | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; kernel-x86_64/scripts/smoke-test.sh | delivered |
| REQ-GFX-002 | Framebuffer console over scatter-gather backing: public-domain 8x8 font double-struck to 8x16 cells, cursor/wrap/scroll/backscreen-blank state machine with fail-closed control bytes, pixel-exact host proofs, 150-page single-command ATTACH_BACKING into REAL frames, TRANSFER+FLUSH of the rendered frame, and DETACH that REVOKES every page's DMA registration | ADR-058 | kernel-core/src/fbcon.rs; kernel-core/src/font8x8.rs; kernel-core/src/dma.rs; kernel-core/src/virtq.rs; kernel-core/src/virtiogpu.rs | kernel-core/tests/fbcon.rs | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; kernel-x86_64/scripts/smoke-test.sh | delivered |
| REQ-CON-001 | Interactive console: fail-closed line editor (printable ASCII only, bounded, refused bytes never echoed) + command dispatch over the namespace, on every target; a session keeps an exit-code contract via `halt` | ADR-044 | kernel-core/src/shell.rs; kernel/src/shellio.rs; kernel-riscv64/src/shellio.rs; kernel-x86_64/src/shellio.rs | kernel-core/tests/shell.rs; kernel-core/src/shell.rs; scripts/conformance.sh | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; scripts/vm-e2e-x86.sh; scripts/console-e2e.sh | delivered |
| REQ-CON-002 | Interrupt-driven console input on ALL THREE targets (GICv2 / 8259A+IRQ4 / PLIC), over a bounded ring with a DROP-NEWEST overflow policy — a typed command is never rewritten — and losses counted and reported | ADR-045 | kernel-core/src/conring.rs; kernel/src/conirq.rs; kernel-x86_64/src/conirq.rs; kernel-riscv64/src/conirq.rs | kernel-core/tests/conring.rs; kernel-core/src/conring.rs; scripts/conformance.sh | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; scripts/vm-e2e-x86.sh; scripts/console-e2e.sh | delivered |
| REQ-CON-004 | A line editor an operator can work in: escape sequences PARSED (bounded CSI state machine, no byte inside a sequence reaches the line), cursor movement and mid-line editing, bounded history, Tab completion over commands and live object names; one input alphabet shared by the UART and the scancode decoder | ADR-050 | kernel-core/src/shell.rs; kernel-core/src/keymap.rs; kernel-core/src/conring.rs; kernel-x86_64/src/conirq.rs | kernel-core/tests/shell.rs; kernel-core/tests/keymap.rs; kernel-core/src/shell.rs; scripts/conformance.sh | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; scripts/vm-e2e-x86.sh; scripts/console-e2e.sh; scripts/keyboard-e2e.sh | delivered |
| REQ-CON-005 | A working command set over the namespace: copy, rename, append (one transaction), create-if-absent that never truncates, prefix search, text and non-text reading, device flush, session history, and a platform reset on all three targets that admits failure instead of hanging | ADR-051 | kernel-core/src/shell.rs; kernel/src/shellio.rs; kernel-riscv64/src/shellio.rs; kernel-riscv64/src/sbi.rs; kernel-x86_64/src/shellio.rs; kernel-x86_64/src/smp.rs | kernel-core/tests/shell.rs; kernel-core/src/shell.rs; scripts/conformance.sh | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; scripts/vm-e2e-x86.sh; scripts/console-e2e.sh | delivered |
| REQ-CON-006 | An idle console waits instead of spinning: when the input ring is empty the console loop parks the CPU on the interrupt that fills it (`wfi` on aarch64/RISC-V, `sti; hlt` on x86-64), defaulted to the old spin so a polled target can never be parked forever | ADR-056 | kernel-core/src/shell.rs; kernel/src/shellio.rs; kernel-riscv64/src/shellio.rs; kernel-x86_64/src/shellio.rs | kernel-core/tests/shell.rs | scripts/console-e2e.sh; scripts/comparative-bench.sh | delivered |
| REQ-PERF-001 | Comparative benchmark against a real Linux kernel on the same host, same emulator, same emulation mode, booting to the same thing (an interactive serial shell), with the substrate stated and the losses reported alongside the wins | ADR-056 | scripts/comparative-bench.sh | scripts/comparative-bench.sh | scripts/comparative-bench.sh | delivered |
| REQ-PERF-002 | The machine measures itself: an arch-independent benchmark suite over the shared Hal clock seam runs in EVERY target's boot gate — authority checks, capability-checked delivery, journal commits, scheduler dispatches, console formatting — with throughput REPORTED (ns on calibrated clocks; raw ticks labelled uncalibrated on x86-64, `<1` below clock resolution) and everything structural GATED: work really done, authority unbroken mid-window, commits read back byte-for-byte, steady state proven by a zero-materialization rerun window, exactly-fair dispatch, byte-exact console arithmetic, identical-work determinism, and the summary rendered GLYPH-EXACT onto real framebuffer pages (wrap + scroll contracts) so the numbers are verified on BOTH consoles — plus a cross-OS typed-workload leg driving identical paced echo round-trips into every guest that reaches a shell on the same emulator | ADR-064 | kernel-core/src/bench.rs; kernel/src/main.rs; kernel-riscv64/src/main.rs; kernel-x86_64/src/main.rs; scripts/comparative-bench.sh | kernel-core/tests/bench.rs | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; kernel-x86_64/scripts/smoke-test.sh; scripts/comparative-bench.sh | delivered |
| REQ-PM-001 | The power/performance contract: frequency domains with honest discrete ladders; the governor range free to any caller; the overclock band gated by live per-domain elevation grants (attenuated delegation, cascading revocation with an immediate clamp back to nominal); the thermal envelope absolute by construction; a tick-exact trip cooldown that refuses even valid grants; a demand governor that never overclocks and never parks demanded silicon; exact idle residency + wake-latency accounting; legal-arcs-only device power (D3→D1 refused); every accepted act and refusal in a bounded monotonic audit ledger | ADR-076 | kernel-core/src/pm.rs; kernel/src/main.rs; kernel-riscv64/src/main.rs; kernel-x86_64/src/main.rs | kernel-core/tests/pm.rs | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; kernel-x86_64/scripts/smoke-test.sh | delivered |
| REQ-GFX-003 | The composition contract: surfaces minted with possession-based owner tokens gating every op; exact scanout clipping (fully-off placements refused); owner-controlled painter z-order; size-honest packed buffer fills; same-frame visibility of placement changes through bounded coalescing screen damage (clear-then-repaint through the z-order); a zero-write quiet frame with per-frame cost reporting; bit-identical determinism; the composed frame reaches the display device over the virtio-gpu flush path - real scatter-gather backing pages via a refusal-counting ComposeSink, one transfer+flush per changed frame, zero device commands on a quiet frame, teardown revoking every page with the device confirming the end | ADR-077; ADR-078 | kernel-core/src/compositor.rs; kernel-core/src/fbcon.rs; kernel-core/src/virtiogpu.rs; kernel/src/main.rs; kernel-riscv64/src/main.rs; kernel-x86_64/src/main.rs | kernel-core/tests/compositor.rs; kernel-core/tests/compfb.rs | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; kernel-x86_64/scripts/smoke-test.sh | delivered |
| REQ-MM-007 | Kernel stack guard pages (the page below every kernel stack has no translation) | ADR-040 | kernel/src/vm.rs; kernel-riscv64/src/vm.rs; kernel-x86_64/src/gdt.rs; kernel-x86_64/src/kmap.rs | kernel/src/vm.rs; kernel-riscv64/src/vm.rs; kernel-x86_64/src/vm.rs | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; scripts/vm-e2e-x86.sh | delivered |
| REQ-MM-008 | Address-space layout model (declared regions, validated: disjoint, aligned, guarded, no null page) + VA 0 never translates | ADR-040 | kernel-core/src/layout.rs; kernel/src/vm.rs; kernel-riscv64/src/vm.rs; kernel-x86_64/src/kmap.rs | kernel-core/src/layout.rs; docs/INVARIANT-CONTRACTS.md | scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; scripts/vm-e2e-x86.sh | delivered |
| REQ-FAULT-001 | Page-fault classification model (normalized fault + kind + fail-closed verdict; x86/aarch64/RISC-V decoders) | ADR-039 | kernel-core/src/faultclass.rs; kernel-x86_64/src/idt.rs | kernel-core/tests/faultclass.rs; docs/INVARIANT-CONTRACTS.md | scripts/vm-e2e-x86.sh | delivered |
| REQ-FAULT-002 | Re-entrancy detection for state a trap handler shares with the code it interrupted | ADR-039 | kernel-core/src/reentry.rs; kernel-x86_64/src/idt.rs | kernel-core/tests/reentry.rs; docs/INVARIANT-CONTRACTS.md | scripts/vm-e2e-x86.sh | delivered |
| REQ-QUAL-002 | Source quality gate: fmt + clippy -D warnings + advisory scan + license allow-list + SBOM, pinned toolchains, both pipelines | ADR-013 | scripts/quality-gate.sh; scripts/sbom.py; docs/TOOLCHAIN.md | scripts/quality-gate.sh | - | delivered |
| REQ-BOOT-001 | Secure boot + chain of trust (firmware→bootloader→kernel measured chain, hardware root) | ADR-025 | aletheia/src/provenance.rs; aletheia/src/crypto.rs | aletheia/tests/component_signing.rs | - | partial |
| REQ-BOOT-002 | Asymmetric component provenance (ed25519, public-key-only verifier, root→signing key hierarchy), now CROSSED INTO INSTALLATION AND LIVE (ADR-067): chain-verified installs record full provenance evidence, the launch gate re-judges evidence against current trust so signer revocation goes live at the next launch, and the spawn path passes the same gate | ADR-025; ADR-067 | aletheia/src/provenance.rs; aletheia/src/crypto.rs; aletheia/src/syscore.rs | aletheia/src/provenance.rs; aletheia/tests/component_supply_chain.rs; aletheia/tests/component_signing.rs | - | delivered |
| REQ-SCHED-002 | Task-lifecycle invariants (Finished terminal, one Running, rotation count exact, unknown-task events are no-ops) | ADR-020 | kernel-core/src/sched.rs | kernel-core/tests/sched.rs; docs/INVARIANT-CONTRACTS.md | - | delivered |
| REQ-REL-002 | Task supervisor: a user fault terminates that task and the system continues (kernel faults still escalate) | ADR-042 | kernel-core/src/supervisor.rs; kernel-x86_64/src/usermode.rs | kernel-core/tests/supervisor.rs | scripts/vm-e2e-x86.sh | delivered |
| REQ-REL-001 | Fault recovery / supervision | ADR-026 | - | - | - | deferred |
| REQ-QUAL-004 | Second-hypervisor qualification: the SAME x86-64 image boots on an independent implementation of the platform contract (Oracle VirtualBox — its own EFI, its own ACPI, SATA/AHCI, no isa-debug-exit), judged by serial-log marker parity with the QEMU gate, with device families VirtualBox cannot emulate named as SKIP | ADR-046 | scripts/vm-e2e-vbox.sh; kernel-x86_64/scripts/mkesp.py | scripts/vm-e2e-vbox.sh | scripts/vm-e2e-vbox.sh | delivered |
| REQ-QUAL-005 | Structured machine-readable invariant markers: every VM gate turns the boot log's per-suite sentences into a tag=count MAP and holds it against an expected family/count table declared in that gate (measured per target; aarch64 ≡ RISC-V identical by assertion), failing on missing, unexpected (new suites join deliberately), or changed families — then prints one GATE-MARKERS-V1 line CI collects without parsing prose | ADR-061 | scripts/lib-markers.sh; scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; kernel-x86_64/scripts/smoke-test.sh | scripts/lib-markers.sh; scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; kernel-x86_64/scripts/smoke-test.sh | scripts/check-ci-parity.sh | delivered |
| REQ-QUAL-006 | Fault-injection coverage: a scripted adversary device (refused reads/writes/flushes at any protocol position; refused writes mutate nothing, refused reads leave buffers unwritten) driven EXHAUSTIVELY through every position of the journal's commit and recovery protocols — each refusal ends in exactly one promised world (pivot rule: pre-pivot old with nothing replayed, post-pivot new via idempotent recovery), failed flushes surface as Err(Device), and retry-on-healthy completes the transaction | ADR-062 | kernel-core/src/faultdev.rs; kernel-core/tests/faults.rs | kernel-core/tests/faults.rs | - | delivered |
| REQ-QUAL-007 | Long-running soak: four lifecycle campaigns — journal transactions rewritten in place, filesystem namespace mutations audited after EVERY operation against the full structural contract, capability grants churned share/attenuate/write/read/revoke with zero-copy observed every cycle, task generations over both arch-independent schedulers — gated on scale-free properties (journaled bytes read back under volume, recovery mid-soak replays idempotently, fail-closed refusals hold at volume, Finished never dispatches again, drains exactly-once, same seed replays identically) with the ALLOCATION-FREE churn window held EXACTLY on each kernel target's own heap meter and throughput reported, never gated | ADR-063 | kernel-core/src/soak.rs; scripts/vm-e2e.sh; scripts/vm-e2e-riscv.sh; kernel-x86_64/scripts/smoke-test.sh | kernel-core/tests/soak.rs | scripts/conformance.sh | delivered |
| REQ-SVC-002 | Host-independent Service API transport: the IPC boundary names no platform API, with per-host backends (Unix-domain socket; token-guarded loopback rendezvous on Windows) behind one Aletheia-owned seam | ADR-047 | aletheia/src/transport.rs; aletheia/src/service.rs | aletheia/src/transport.rs; aletheia/tests/conformance.rs | - | delivered |
| REQ-FAULT-003 | Adversarial ring-3 entry: a real illegal opcode (#UD) and a real privileged instruction (#GP) executed at CPL 3 reach their own vectors, are classified as user faults, and are contained by the supervisor — plus a full 15-register trap-frame round-trip through the real trap assembly | ADR-039 | kernel-x86_64/src/usermode.rs; kernel-x86_64/src/idt.rs | kernel-x86_64/src/usermode.rs | scripts/vm-e2e-x86.sh; scripts/vm-e2e-vbox.sh | delivered |

> Deferred rows carry an ADR (the phased plan) but no code — by design (ADR-010: no blind hardware
> code). They become `partial`/`delivered` only when a real implementation and test land, at which
> point this check begins enforcing their evidence.
